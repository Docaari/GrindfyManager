/**
 * Test-Writer (Modo TDD - Red Phase)
 *
 * Sprint Bankroll-3 — RF-12: queryKey hooks F4 inclui userId
 *
 * Spec: Docs/specs/sprint-bankroll-3.md (RF-12)
 *
 * STATUS: SKIPPED — F4 (feature/f4-primedope) ainda nao foi merged em main.
 * Hooks useDayDetail, usePrimedopeRuns, usePrimedopeSimulation NAO existem
 * em feature/bankroll-3.
 *
 * Quando F4 for merged: rebase + remover skip + implementar 6 testes:
 *   - useDayDetail queryKey inclui userId
 *   - useDayDetail enabled !!userId
 *   - usePrimedopeRuns queryKey inclui userId
 *   - usePrimedopeRuns enabled !!userId
 *   - usePrimedopeSimulation queryKey inclui userId
 *   - usePrimedopeSimulation enabled !!userId
 *
 * Tracker: ver memory/session_2026-04-29-f4.md.
 */

import { describe, it } from 'vitest';

describe.skip('RF-12: queryKey userId — depende de F4 merged em main', () => {
  it('placeholder — implementar apos rebase com F4', () => {
    // Quando F4 estiver disponivel, escrever os 6 testes documentados.
  });
});
