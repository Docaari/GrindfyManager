# Destino: .env.example na raiz do PROJETO do usuário.
# Maio 2026.
#
# Template de variáveis de ambiente. Renomeie ou copie para .env e preencha
# com valores reais (que ficarão no .gitignore e nunca serão committados).
#
# Regras:
# 1. NUNCA commite o .env real
# 2. Use valores fake aqui que sirvam de documentação
# 3. Variáveis sensíveis em produção via secret manager (não .env)
# 4. Cada variável tem um comentário explicando para que serve

# ─── Aplicação ────────────────────────────────────────────────────────────
NODE_ENV=development                    # development | staging | production
PORT=3000                               # porta do servidor HTTP
APP_URL=http://localhost:3000           # URL pública da aplicação

# ─── Banco de dados ───────────────────────────────────────────────────────
DATABASE_URL=postgresql://user:password@localhost:5432/mydb
DATABASE_POOL_MAX=10                    # tamanho máximo do pool de conexões

# ─── Cache / sessões ──────────────────────────────────────────────────────
REDIS_URL=redis://localhost:6379

# ─── Autenticação ─────────────────────────────────────────────────────────
JWT_SECRET=                             # gerar com: openssl rand -hex 32
JWT_EXPIRES_IN=7d
BCRYPT_ROUNDS=12

# ─── Serviços externos ────────────────────────────────────────────────────
# Email (escolha um provedor)
# SENDGRID_API_KEY=
# RESEND_API_KEY=
# AWS_SES_REGION=us-east-1

# Pagamentos
# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_WEBHOOK_SECRET=whsec_...

# Storage de objetos
# S3_BUCKET=
# S3_ACCESS_KEY_ID=
# S3_SECRET_ACCESS_KEY=
# S3_REGION=us-east-1

# Observabilidade
# SENTRY_DSN=
# LOG_LEVEL=info                        # debug | info | warn | error

# ─── Feature flags / configs específicas do projeto ──────────────────────
# FEATURE_X_ENABLED=false
