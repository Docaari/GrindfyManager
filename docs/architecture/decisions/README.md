# Architecture Decision Records (ADRs)

Indice de decisoes arquiteturais do projeto Grindfy Manager.

Cada ADR documenta o contexto, opcoes consideradas, decisao tomada e consequencias de uma decisao tecnica significativa. ADRs nunca sao deletados — sao marcados como "Deprecado" ou "Substituido" quando uma decisao muda.

## Indice

| ADR | Titulo | Status | Data |
|-----|--------|--------|------|
| [001](001-auth-jwt.md) | Usar JWT com refresh token para autenticacao | Aceito | 2025-01-01 |
| [002](002-neon-serverless.md) | Usar Neon Serverless PostgreSQL como banco de dados | Aceito | 2025-01-01 |
| [003](003-monolith-architecture.md) | Monolito Express servindo API + SPA na mesma porta | Aceito | 2025-01-01 |
| [007](007-blocknote-editor.md) | Usar BlockNote como editor block-based para pagina Estudos | Aceito | 2026-03-21 |
| [008](008-late-reg-alerts-architecture.md) | Arquitetura de alertas de late registration no Grind Live | Aceito | 2026-03-21 |
| [009](009-tournament-library-separate-table.md) | Criar tournament_library como tabela separada de tournament_templates | Aceito | 2026-03-21 |
| [010](010-profile-off-fourth-state.md) | Perfil OFF como 4o estado (nao substituindo C) | Aceito | 2026-03-21 |
| [011](011-react-beautiful-dnd-choice.md) | Usar react-beautiful-dnd para drag-and-drop na grade | Aceito | 2026-03-21 |
| [012](012-suprema-dedup-strategy.md) | Estrategia de deduplicacao Suprema: externalId + nome+site+buyIn | Aceito | 2026-03-21 |
| [013](013-eliminate-planning-dialog.md) | Eliminar PlanningDialog e redistribuir funcionalidades | Aceito | 2026-03-21 |
| [014](014-addon-rea-modelagem.md) | Modelar Add-on e Re-entry como flags ortogonais (nao expandir enum `type`) | Aceito | 2026-04-23 |
| [015](015-scoring-linear-vs-ml.md) | Combinacao linear ponderada com Bayesian shrinkage para Tournament Selector (vez de ML) | Aceito | 2026-04-23 |
| [016](016-bundle-aggregation-pattern.md) | Endpoint agregado `/api/analytics/player-bundle` em vez de 7 chamadas paralelas | Aceito | 2026-04-23 |
| [017](017-bankroll-snapshot-vs-derived.md) | Banca em tabela `bankroll_snapshots` (snapshots explicitos) em vez de derivar | Aceito | 2026-04-24 |
| [018](018-bankroll-tolerance-hardcoded.md) | Tolerancia de bankroll 1.5x hardcoded (sem config por usuario) | Aceito | 2026-04-24 |
| [019](019-coach-prompt-cache-strategy.md) | Coach prompt em 2 blocos (estatico cacheado + dinamico) | Aceito | 2026-04-24 |
| [020](020-coach-rate-limit-rolling-24h.md) | Rate limit do Coach em janela rolling de 24h (nao calendar-day) | Aceito | 2026-04-24 |
| [021](021-coach-model-selection-via-env.md) | Modelos do Coach parametrizados via env com defaults atualizados | Aceito | 2026-04-24 |
| [022](022-coach-confidence-tags-inline-vs-structured.md) | Confidence tags inline textuais (vez de JSON estruturado ou tool use) | Aceito | 2026-04-24 |
| [023](023-coach-tool-registry-pattern.md) | Tool registry pattern modular por dominio + index central (Coach-2A) | Aceito | 2026-04-24 |
| [024](024-coach-tool-result-wrapping.md) | Tool result wrapping em JSON estruturado anti prompt injection (Coach-2A) | Aceito | 2026-04-24 |
| [025](025-coach-page-context-zod-whitelist.md) | Page context com Zod discriminated union por route (Coach-2A) | Aceito | 2026-04-24 |
| [026](026-coach-continuation-loop-limit.md) | Continuation loop limit de 5 tool calls por turn (Coach-2A) | Aceito | 2026-04-24 |
| [027](027-warmup-soft-gate-with-override.md) | Gate Go/No-Go do warm-up como SOFT (warning + double-confirm) em vez de HARD (Sprint W-1) | Aceito | 2026-04-25 |
| [028](028-warmup-rituals-vs-preparation-logs.md) | Criar nova tabela `warmup_rituals` em vez de estender `preparation_logs` (Sprint W-1) | Aceito | 2026-04-25 |
| [029](029-warmup-no-dual-write-legacy-logs.md) | Nao fazer dual-write em `preparation_logs` durante a transicao (Sprint W-1) | Aceito | 2026-04-25 |
| [030](030-warmup-telemetry-client-only-w1.md) | Telemetria do warm-up Sprint W-1 e client-only via console.log (sem persistencia server-side) | Aceito | 2026-04-25 |
| [031](031-tournament-types-orthogonal-model.md) | Modelo ortogonal de tipos de torneio (type primario + modificadores booleanos isFlight/isLive) | Aceito | 2026-04-25 |
| [032](032-deprecation-category-column.md) | Deprecation gradual da coluna `tournaments.category` em 5 sprints | Aceito | 2026-04-25 |
| [033](033-fx-rate-convention-units-per-usd.md) | Convencao oficial de `exchangeRates`: unidades nativas equivalentes a 1 USD | Aceito | 2026-04-26 |
| [034](034-multi-wallet-with-immutable-fx.md) | Modelo Multi-Wallet com FX Historico Imutavel | Aceito | 2026-04-26 |
| [035](035-bankroll-v1-to-v2-migration.md) | Compatibilidade v1->v2 e Migracao de Snapshots de Bankroll | Aceito | 2026-04-26 |
| [036](036-tickets-effective-buyin.md) | Buy-in Efetivo Zero e ROI Individual Null em Torneios via Ticket | Aceito | 2026-04-26 |
| [037](037-tickets-table-vs-jsonb.md) | Tabela `tickets` Separada vs JSON Column em `tournaments` | Aceito | 2026-04-26 |
| [038](038-wallet-tx-optimistic-concurrency.md) | Optimistic concurrency em wallet transactions via `expectedPreviousBalance` | Aceito | 2026-04-26 |
| [039](039-rakeback-as-wallet-tx-reason.md) | Rakeback como `reason='rakeback'` em wallet_transactions (sem novo endpoint, sem nova tabela) | Aceito | 2026-04-26 |
| [040](040-session-end-wallet-reconciliation.md) | Reconciliacao de banca ao fim da sessao via passo intermediario (endpoint batch fail-fast, idempotente, reuso de ADR-017/034/038, sem schema delta) | Proposto | 2026-04-26 |
| [041](041-cooldown-dedicated-spec-and-schema.md) | Cool-down em spec dedicada com schema novo (`cooldown_logs` + `starred_hands`) | Aceito | 2026-04-26 |
| [042](042-cooldown-coach-tool-registry.md) | Tool `coach.read_cooldown_history` no Tool Registry com sanitizer agregador + prompt block cacheable | Aceito | 2026-04-26 |
| [043](043-coach-page-context-cooldown-log.md) | Page context `cooldownLog` — extensao da Zod discriminated union do Coach | Aceito | 2026-04-26 |
| [044](044-session-tournament-satellite-fields.md) | Campos de satelite em `session_tournaments` | Aceito | 2026-04-26 |
| [045](045-session-end-wallet-tie-break.md) | Session-end reconciliation — site-to-wallet tie-break policy | Aceito | 2026-04-26 |
| [046](046-session-wallet-snapshots-table.md) | Tabela `session_wallet_snapshots` para persistencia da reconciliacao por sessao | Aceito | 2026-04-26 |
| [047a](047-summary-inline-reconcile.md) | Reconciliacao de wallets inline no `SessionSummaryModal` | Aceito | 2026-04-26 |
| [047b](047-tts-browser-native-vs-cloud.md) | TTS browser-native (SpeechSynthesis) vs cloud TTS | Aceito | 2026-04-27 |
| [048a](048-tts-priority-queue.md) | Priority queue para multi-tabling TTS (FLUSH inteligente + cap 3 itens / 30s) | Aceito | 2026-04-27 |
| [048b](048-wallets-eligibility-platforms-played.md) | Eligibility de wallets = todas plataformas jogadas (cadastro inline obrigatorio) | Aceito | 2026-04-26 |
| [049](049-tts-privacy-default.md) | Privacy default `ttsRedactBuyIn=true` ("Modo discreto" ativo por padrao) | Aceito | 2026-04-27 |
| [050](050-tts-state-module-level.md) | TTS state em module-level singleton (nao Context, nao Zustand) | Aceito | 2026-04-27 |
| [051a](051-spot-screenshots-storage.md) | Spot screenshots: storage local em F2 (S3 deferido para F3) | Proposto | 2026-04-27 |
| [051b](051-stats-analyzer-layout-schema.md) | Stats Analyzer: layout JSON shape (`hud_layouts.sections`) | Aceito | 2026-04-26 |
| [052a](052-spot-screenshots-ownership.md) | Spot screenshots: ownership middleware em GET /image | Proposto | 2026-04-27 |
| [052b](052-stats-analyzer-coach-tool-integration.md) | Stats Analyzer: Coach AI tool `read_user_hud_stats` | Aceito | 2026-04-26 |
| [053](053-spot-screenshots-cron.md) | Cron diario de purge de spot screenshots via `node-cron` em F2 (scheduler externo em F3) | Proposto | 2026-04-27 |
| [054](054-primedope-external-provider-vs-native-engine.md) | PrimeDope como provider externo (interim) vs engine Monte Carlo nativo (Sprint F4) | **Substituido por ADR-211** | 2026-04-28 |
| [055](055-tracker-stub-vs-analytics-events-table.md) | `tracker.ts` stub minimo via console.log vs tabela `analytics_events` persistida (Sprint F4) | Aceito | 2026-04-28 |
| [056](056-onboarding-dismiss-localstorage.md) | Onboarding educativo dismiss via `localStorage` vs coluna `users.preferences` JSONB (Sprint F4) | Aceito | 2026-04-28 |
| [088](088-hud-stat-targets-knowledge-base.md) | hud_stat_targets knowledge base global + override inline (Sprint F4 stats-analyzer, ex-057) | Aceito | 2026-04-29 |
| [089](089-hud-snapshot-sample-size-per-stat.md) | sample size per stat em snapshots.values com 3 formatos back-compat (Sprint F4 stats-analyzer, ex-058) | Aceito | 2026-04-29 |
| [090](090-tournament-series-single-source-of-truth.md) | Tournament Series como single source of truth — deprecar flags inline ADR-031 (Sprint Flight-1) | Aceito | 2026-05-02 |
| [091](091-stack-mode-enum-single-combined.md) | Stack mode enum: `single` \| `combined` (best-stack fora MVP, defer Flight-2) | Aceito | 2026-05-02 |
| [103](103-library-access-requests-table.md) | Tabela `library_access_requests` + UNIQUE INDEX parcial WHERE status='pending' (Sprint UX-Biblioteca-1) | Aceito | 2026-05-03 |
| [104](104-library-video-speed-global-localstorage.md) | Velocidade video global via localStorage `library-video-speed` (Sprint UX-Biblioteca-1) | Aceito | 2026-05-03 |
| [105](105-auto-navigation-countdown-pattern.md) | Padrao auto-navegacao countdown 5s + cancel sticky (NextLessonCTA, Sprint UX-Biblioteca-1) | Aceito | 2026-05-03 |
| [110](110-news-feed-ranking-and-zoning.md) | News feed `/api/news/feed` ranqueado server-side + 4 zonas semanticas Home + read-state localStorage (Sprint home-reform-3) | Aceito | 2026-05-03 |
| [111](111-coach-lesson-recommendation-schema.md) | Tabela dedicada `coach_lesson_recommendations` com UNIQUE `(userId, weekStartDate)` + ciclo semanal previsivel (home-reform-4 item 4) | Aceito | 2026-05-03 |
| [112](112-coach-recommendation-cron-strategy.md) | Cron node-cron in-process `0 6 * * 1` America/Sao_Paulo + idempotencia em 3 camadas + endpoint admin regenerate (home-reform-4 item 4) | Aceito | 2026-05-03 |
| [113](113-coach-recommendation-fallback-tiers.md) | Algoritmo Coach IA + fallback determinista em 5 tiers (short-circuit -> Coach -> leak->tag -> popular -> recent -> null) (home-reform-4 item 4) | Aceito | 2026-05-03 |
| [114](114-coach-recommendation-consume-tracking.md) | Consume tracking via 30s OR 80% playback + query param `?source=home-coach-rec&recId=...` + library_events `coach_recommend` (home-reform-4 item 4) | Aceito | 2026-05-03 |
| [115](115-coach-recommendation-prompt-cache.md) | Prompt cache 2 blocos `cache_control: ephemeral` (system + catalog) + cap 200 lessons + hit rate alvo > 90% (home-reform-4 item 4) | Aceito | 2026-05-03 |
| [116](116-user-focus-stats-schema-monthly.md) | Schema `user_focus_stats` com escopo mensal + UNIQUE composta + reset implicito ao virar mes (home-reform-4 item 7) | Aceito | 2026-05-03 |
| [117](117-study-sessions-theme-id-opcao-c.md) | `study_sessions.theme_id` nullable + sem back-fill historico + accountability futura (Opcao C aprovada) (home-reform-4 item 7) | Aceito | 2026-05-03 |
| [118](118-focus-stats-card-zone-estudos.md) | `FocusStatsCard` em nova zona "Estudos" no Home, entre Performance e Sinal Externo (home-reform-4 item 7) | Aceito | 2026-05-03 |
| [106-news](106-news-feed-grok-integration.md) | News feed: integracao xAI Grok + opt-in granular (Sprint News-1) | **SUPERSEDED por ADR-107-news** | 2026-05-03 |
| [107-news](107-news-rss-x-search-refactor.md) | Refatorar news provider para RSS/HTML scrapers + xAI Live Search com `sources[].type='x'`; pipeline dedupe 3-layer; cron weekly UTC; supersedes ADR-106-news (Sprint News-3) | Aceito | 2026-05-04 |
| [121](121-system-fx-rates-global-table.md) | Tabela global `system_fx_rates` (PK composta `(date, currency)`) + override per-user opcional via `users.exchangeRates` + cascata estendida no `fxResolver` (Sprint FX-1) | Aceito | 2026-05-05 |
| [122](122-fx-multi-source-fallback-chain.md) | Multi-source fallback chain: BCB PTAX primary BRL (auditabilidade fiscal) + frankfurter primary EUR (ECB coverage) + cross-fallback assimetrico BCB→frankfurter para BRL (Sprint FX-1) | Aceito | 2026-05-05 |
| [123](123-fx-cron-daily-17utc.md) | Cron diario `0 0 17 * * *` UTC (14:00 SP, apos PTAX + ECB publicados) + idempotencia DB-level via `ON CONFLICT (date, currency) DO NOTHING` + force refresh reutiliza pipeline (Sprint FX-1) | Aceito | 2026-05-05 |
| [124](124-break-auto-open-clock-aligned-brt.md) | Break Auto-Open clock-aligned em America/Sao_Paulo (BRT, UTC-3 sem DST) com toggle persistente em `user_settings.break_auto_open_enabled` (default true) + dedupe via hour key `YYYY-MM-DD-HH` + helpers puros TDD-friendly (Sprint Grind-Live Break Auto-Open) | Aceito | 2026-05-05 |
| [125](125-coach-tabs-consolidation.md) | Consolidar Flight + Variance em abas dentro de `/coach` com persistencia via `?tab=` + redirect `/flight -> /coach?tab=flights` + alias testid legacy `grade-tab-selector` via wrapper `display:contents` (Sprint coach-page-reform-1) | Aprovado | 2026-05-07 |
| [126](126-study-sessions-v2-new-table.md) | Criar `study_sessions_v2` como tabela nova; manter `study_sessions` legado read-only; FocusStatsCard composer UNION ALL durante deprecation gradual (Sprint estudos-habito-1) | Aprovado | 2026-05-08 |
| [127](127-study-themes-hybrid-taxonomy.md) | Theme taxonomy hibrida: ~30 themes curated (seed) + user custom + linked_stats/linked_lessons jsonb + lazy seed per-user (Sprint estudos-habito-1) | Aprovado | 2026-05-08 |
| [128](128-study-streak-algorithm.md) | Algoritmo streak diario state machine + 2 freezes/mes silenciosos + cron 00:05 UTC + lazy reset + race-safe via FOR UPDATE (Sprint estudos-habito-1) | Aprovado | 2026-05-08 |
| [129](129-focus-stats-bar-visibility-granular.md) | Visibility granular per-placement em `homeLayoutSettings.focusStatsVisibility` nested object + lazy back-fill via storage layer (Sprint estudos-habito-1) | Aprovado | 2026-05-08 |
| [130](130-auto-lesson-idempotency-window.md) | Idempotency auto_lesson via janela rolling 24h + indice parcial `idx_ssv2_user_lesson_partial` + FOR UPDATE (sem UNIQUE constraint) (Sprint estudos-habito-1, prep Sprint 2) | Aprovado | 2026-05-08 |
| [131](131-auto-lesson-trigger-client-side.md) | Auto-trigger Mux 80% via hook client-side `useLessonStudyAutoLog` + cap/validacao/idempotency server-side; webhook Mux e listener em library_lesson_progress descartados (Sprint estudos-coach-biblio-2) | Aprovado | 2026-05-08 |
| [132](132-study-weekly-plans-jsonb.md) | Schema `study_weekly_plans` com `plan_jsonb` array embarcado de dias (vs tabela child de atividades); UNIQUE composite `(user_id, week_start_date)` + UPSERT atomico para idempotency cron+manual (Sprint estudos-coach-biblio-2) | Aprovado | 2026-05-08 |
| [133](133-coach-session-insights-table-vs-memory.md) | Cache `coach_session_insights` em tabela PG dedicada com `expires_at` 24h (vs Map in-memory ou Redis); UNIQUE `grind_session_id` + INSERT ON CONFLICT DO UPDATE race-safe (Sprint estudos-coach-biblio-2) | Aprovado | 2026-05-08 |
| [134](134-coach-tools-study-plan-and-session-insights.md) | Coach tools `coachStudyPlan` + `coachSessionInsights` no registry modular (ADR-023) com servicos orquestradores separados; whitelist enforce anti-hallucinacao (Sprint estudos-coach-biblio-2) | Aprovado | 2026-05-08 |
| [135](135-errata-adr-126-mode-lesson-theme-id-nullable.md) | Errata ADR-126: `mode='lesson'` em `study_sessions_v2` aceita `theme_id NULL` quando aula nao tem mapping em `study_themes.linked_lessons`; migration 0057 NO-OP (DROP CONSTRAINT IF EXISTS + COMMENT update) (Sprint estudos-coach-biblio-2) | Aprovado | 2026-05-08 |
| [136](136-spot-reentry-cards-sm2-simplified.md) | Tabela `spot_reentry_cards` + algoritmo SM-2 simplificado (4 grades again/hard/good/easy, caps interval [1,120] + ease [1.3, 3.0], default ease 2.5); rejeita FSRS/Leitner/lib externa em favor de hand-rolled determinista (Sprint spot-anki-reentry-3) | Aprovado | 2026-05-08 |
| [137](137-spot-reentry-daily-auto-create-cap.md) | Cap diario 5 cards auto-criados por user/dia para `source='drill_gto_difficult_spot'` via cron (anti-overflow R5 research, manual unlimited, bulk-coach cap separado 20/request) (Sprint spot-anki-reentry-3) | Aprovado | 2026-05-08 |
| [138](138-starred-hands-relax-fk-nullable.md) | Relax `starred_hands.session_id` + `session_tournament_id` para NULLABLE (suportar drill spots orfaos do cron); estende enums `captured_during` (+drill_gto) e `type` (+drill); audit obrigatorio em `storage.ts` queries (R6 mitigation) (Sprint spot-anki-reentry-3) | Aprovado | 2026-05-08 |
| [139](139-spot-reentry-initial-interval-by-source.md) | Initial interval por source: `manual_add`+`drill_gto_difficult_spot`=1d sempre; `coach_session_insight` (decision_correct=false → 1d / confidence_level<=2 → 2d / default → 1d); ease inicial sempre 2.5 (Sprint spot-anki-reentry-3) | Aprovado | 2026-05-08 |
| [140](140-coach-session-insights-spotsToReview-extension.md) | Estender shape `coach_session_insights.insights_jsonb.spotsToReview[]` com 3 campos opcionais (`reentryCandidate?`, `reentryAlreadyActive?`, `reentryReason?`); enrichment stateless server-side a cada GET; backwards compat (Sprint 2 clients ignoram); zero migration SQL (Sprint spot-anki-reentry-3) | Aprovado | 2026-05-08 |
| [143](143-refresh-token-rotation-db.md) | Rotacao de refresh token persistida no DB (Launch Fase 2 security) | Aceito | 2026-05-11 |
| [144](144-cron-advisory-lock-pattern.md) | Advisory lock pattern para crons in-process (Launch Fase 3 escalabilidade) | Aceito | 2026-05-11 |
| [145](145-errata-coach-tools-registry-canonical-state.md) | Errata aos ADRs 023/024/042/052b/086: estado canonico do registry de Coach Tools pos-AI-0A (17 tools + 1 alias deprecado); enums corrigidos (`speed: Normal\|Turbo\|Hyper`, `category: +Satellite`, `groupBy: +fieldSize`, `period: +180d`); stubs `find_top_leaks`/`simulate_bankroll_scenario` removidos; `query_dimension.groupBy:'fieldSize'` -> `getAnalyticsByField`; filtro `__stub` defensivo mantido (Sprint AI-0A) | Aceito | 2026-05-12 |
| [146](146-coach-write-tools-confirm-always-v1.md) | Coach write tools: confirmacao obrigatoria na v1 (sem auto-aprovacao), sem `delete_*` tools, `confirm-strict` (`confirmationLevel:'strict'` no descriptor em memoria, NAO coluna em `coach_actions`) para `record_wallet_transaction`; reusa infra Coach-2B (runner + `coach_actions` + endpoints + frontend), zero migration (Sprint AI-0A) | Aceito | 2026-05-12 |
| [147](147-coach-read-tool-service-extraction-pattern.md) | Read tools do Coach: extrair `tournamentScoringService` (DRY — nunca duplicar `computeTournamentScore`; `get_tournament_suggestions`/`explain_tournament_score` + route do Selector reusam); fonte canonica de banca p/ `simulate_bankroll_scenario` = `walletService.getConsolidatedBalance` (USD, FX-aware); unificar blocos de prompt citation/confidence (aceita quebra de cache unica, remove variante backticked — lesson #10) (Sprint AI-0A) | Aceito | 2026-05-12 |
| [148](148-grindfy-ai-consolidation-single-agent-with-lens.md) | **Consolidacao Grindfy AI — agente unico com "lente inicial"** (supersedes a separacao de personas Mental/Tournament/Technical): 3 system prompts → `GRINDFY_AI_BASE` unico; contexto DINAMICO sem gate por `coachType` (agente ve tudo); `coachType` = uma linha de lente no bloco DINAMICO (back-compat, zero migracao); removidos `getMentalPrompt`/`getTournamentPrompt`/`getTechnicalPrompt` + `MENTAL/TOURNAMENT/TECHNICAL_BASE`; tier gate vira so rate limit + tools (acaba `403 tier_locked` por coach; `canAccessCoach` removida/trivializada); quebra unica de cache aceita (lesson #10). Supersedes `ai-coach/adr-001` §3-coaches, emenda gate-by-plan (Sprint AI-0B) | Aceito | 2026-05-12 |
| [149](149-coach-page-context-plugged-and-expanded.md) | Page context **plugado de fato** no `/api/coach/chat` (a infra Zod/sanitizer/formatter existia mas o route nunca lia `req.body.pageContext` e o frontend nunca enviava) + **expandido a 10 variantes** (5 originais + 5 novas: `bankroll`, `estudos`, `stats`, `biblioteca`, `upload`, todas `.strict()`); hook novo `useCoachPageContext`; principio "inspecao leve (counts/IDs/abas/filtros/datas) — nao dump; os numeros vem das tools". Estende ADR-025 (Sprint AI-0B) | Aceito | 2026-05-12 |
| [150](150-coach-ai-hub-tabs-layout.md) | Hub `/coach-ai` — tabs URL-persisted `?tab=chat\|reports\|audit\|prefs` (reusa `useTabFromUrl` como `/coach`, ADR-125): Chat (agente unico + chips de lente + page context), Relatorios e avisos (EmptyState — esqueleto; timeline e Fase 1 AI-1B), Historico de acoes (`CoachAuditPanel` — `GET /api/coach/audit`), Preferencias (`CoachPreferencesPanel` — `GET/PUT /api/coach/preferences`, 8 toggles nudge); MiniChat alinhado (agente unico, chips de lente); `UpgradeCoachModal` reproposito (sem "coach X bloqueado"). NAO mexe na pagina `/coach` (GradePlanner). Zero endpoint novo, zero migracao (Sprint AI-0B) | Aceito | 2026-05-12 |
| [151](151-ai-structured-profile-jsonb.md) | **Perfil estruturado do jogador — JSONB em `users.ai_structured_profile`** (fonte confiavel pro prompt + base do ciclo de vida): coluna nullable `DEFAULT '{}'::jsonb` (lesson #7), shape versionado (`schemaVersion: 1` — `{ nivel, nivelConfirmado, metas, focoDoMes, tomPreferido, padroesConhecidos, redesPrincipais, onboarding*, ... }`), normalizado/validado no storage (`server/storage/aiStructuredProfile.ts`); a prosa em `user_ai_profile.content` continua intacta (estruturado e aditivo); entra no bloco STATIC do system prompt; sincronizacao `tomPreferido` ↔ `userCoachPreferences.coachTone` (espelhamento + back-fill lazy); migracao 0065 (separada). Estende AI-002; referencia ADR-015 (rule-based) (Sprint AI-1A) | Aceito | 2026-05-12 |
| [152](152-anti-fadiga-snooze-telemetry-autofreeze-killswitch.md) | **Anti-fadiga do Grindfy AI — snooze + telemetria + auto-congelamento + kill switch** (estende ADR-085): engine ganha 2 checks (1.5 categoria congelada, 1.6 snooze ativo) + check 0 (kill switch global `COACH_NUDGES_ENABLED`, default ON, absoluto); congelamento em `user_coach_preferences.frozen_categories` (JSONB, `NOT NULL DEFAULT '{}'`, migracao 0066); auto-congelamento `nudgeAutoFreeze.ts` (janela 7d / sample minimo 3 / dismiss rate >50%; nao auto-descongela; aviso via `coach_nudge_log` `triggeredByEvent='auto_freeze_notice'`); status `unsubscribed` na log; endpoints REST de telemetria/snooze/unfreeze + `POST /api/admin/coach/freeze-category`; cronRunner nao registra schedules de nudge se kill switch off; nudges ja `sent` permanecem. Estende ADR-084/085/087; precedente `NEWS_FEED_ENABLED` (semantica oposta) (Sprint AI-1A) | Aceito | 2026-05-12 |
| [153](153-onboarding-conversacional-wizard-guiado.md) | **Onboarding conversacional do Grindfy AI — wizard guiado opcional** (nao conversa-LLM real): rota `/coach-ai/onboarding`, modos `full` (6 steps) / `light` (3 steps — re-onboarding p/ perfil vazio); persistencia incremental via `PATCH /api/coach/onboarding` → `users.ai_structured_profile` + `onboardingDraft`; conclusao via `POST /api/coach/onboarding/complete` (seta `onboardingCompletedAt`/`onboardingVersion`, limpa draft, sincroniza tom + grava prefs de nudge); banner persistente em `/coach-ai` e `/inicio` (dismissivel por sessao, "agora nao" → `onboardingSkippedAt`, nao bloqueia); endpoints REST (nao write tools); zero LLM; tier nao afeta. Estende ADR-148/150/151/154 (Sprint AI-1A) | Aceito | 2026-05-12 |
| [154](154-deteccao-nivel-rule-based.md) | **Deteccao de nivel do jogador — heuristica rule-based** (sem ML — segue ADR-015): `estimatePlayerLevel(input)` funcao pura em `server/coach/playerLevel.ts`; 6 niveis (`sem_dados` / `iniciando` / `micro_ascensao` / `mid_consistente` / `high_stakes` / `recreativo_serio`); thresholds em USD (ABI/volume/ROI 90d/redes/idade da conta — `sem_dados` se volume<30 ou ABI null; `high_stakes` ABI≥215+vol≥200; `mid_consistente` ABI 33-215+vol90≥80+ROI90≥-5+conta≥6m; `micro_ascensao` ABI<33+vol90≥80; `recreativo_serio` conta≥12m+vol≥100+ROI+; `iniciando` fallback); tie-break por prioridade; `confidence` (low/medium/high); **confirmacao obrigatoria** (`nivelConfirmado: false` ate o usuario confirmar — o prompt instrui o agente); on-demand only (`GET /api/coach/level-estimate` + step 3 do wizard); usa `getDashboardStats` (filtra §6.1); thresholds calibraveis (Sprint AI-1A) | Aceito | 2026-05-12 |
| [155](155-relatorios-automaticos-report-jobs-runner-tz-aware.md) | **Relatorios automaticos — `report_jobs`/`reports` + job runner timezone-aware + Weekly Report opt-in Pro+** (migracao 0067): `report_jobs` (fila — status `pending`→`running`→`done`/`failed`/`skipped`, `attempts`/`max_attempts`(3)/`next_attempt_at` backoff `15min/1h/4h`, `timezone`+`subscription_plan_at_enqueue` snapshots, UNIQUE `(user_id, report_type, period_start)`, idx `(status, scheduled_for)`) + `reports` (content JSONB `ReportContent` schemaVersion=1 + markdown derivado + `model_used`/`summarizer_model_used`/`cost_usd_estimate`/tokens, status `ready`/`degraded`, `degraded_reason`, UNIQUE `(user_id, report_type, period_start)`); runner `server/jobs/reportJobRunner.ts` — enqueuer `0 * * * *` (`getLocalHour===7` && segunda no fuso do user, `ON CONFLICT DO NOTHING`) + processor `*/15 * * * *` (claim atomico `UPDATE ... WHERE status='pending' RETURNING`, `withAdvisoryLock` ADR-144, revalida elegibilidade, idempotencia via UNIQUE, retry exponencial, fail-soft deterministico apos 3 falhas → `degraded`, job vira `done` nunca `failed`); opt-in `user_coach_preferences.report_weekly_enabled` (`NOT NULL DEFAULT false`, so Pro+ — `isWeeklyReportEligible`); gating `COACH_NUDGES_ENABLED` (sem flag nova); custo+tokens gravados; pacing 200ms; modelo `process.env.COACH_MODEL ?? <canonico>` (ADR-021), sumarizacao Haiku (Q3) (Sprint AI-1B) | Aceito | 2026-05-12 |
| [156](156-aposentadoria-crons-segunda-absorvidos-weekly-report.md) | **Aposentadoria dos 2 crons de segunda — `generateCoachRecommendations` (6h BRT) + `generateWeeklyStudyPlan` (9h UTC) absorvidos pelo Weekly Report** (founder Q10): desliga `cron.schedule("0 6 * * 1",...)` em `cronRunner.ts` + `registerWeeklyStudyPlanCron` em `jobs/index.ts` (modulos viram dead code agendado, comentario "DEPRECATED"); `coach_lesson_recommendations` (chave `getCurrentWeekStartBRT()`) e `study_weekly_plans` (chave `utcMondayOfWeek`) **continuam preenchidas** — agora pelo gerador do Weekly Report (`generateWeeklyReport` chama `recommendLessonForUser`+`createCoachRecommendation` na sec 4 e `generateWeeklyStudyPlan` o service+UPSERT na sec 7, `try/catch` por back-fill); back-compat com `/inicio` cards + `StudyWeeklyPlanCard` (zero mudanca no frontend); 3 nocoes de "semana" coexistem (report=fuso do user/semana que acabou; rec=BRT/semana corrente; plano=UTC/semana corrente); idempotencia cruzada (rollback parcial nao duplica); **trade-off: Free perde a rec de lesson automatica semanal** (Pro+ sem opt-in tambem — rec via chat/tool `recommend_lesson` on-demand; follow-up "cron leve pra todos" documentado se inaceitavel) (Sprint AI-1B) | Aceito | 2026-05-12 |
| [157](157-hub-timeline-report-render-bgapcheck-bimport.md) | **Hub timeline + render de relatorio + B-GAPCHECK/B-IMPORT**: `GET /api/coach/timeline` (merge `reports`+`coach_nudge_log` ordenado timestamp desc, `?limit=`(30/max100)/`?cursor=`, union `TimelineItem` discriminada por `kind:'report'\|'nudge'`, `injectedStorage?`) + `GET /api/coach/reports/:id` (`content`+`markdown`, marca `read_at`, 404/403; opcional `POST .../dismiss`); `ReportsPanel` reescrito (`useQuery(['/api/coach/timeline'])`, EmptyState quando vazio — mantem `data-testid="coach-ai-reports-empty"`); `NudgeCard.tsx` novo (deferido do AI-1A — titulo/body/status + botoes snooze/dismiss/engage chamando endpoints AI-1A; `triggeredByEvent='auto_freeze_notice'` → aviso sem snooze); `WeeklyReportView.tsx` novo em **rota propria `/coach-ai/relatorio/:id`** (vs modal — escolhida pagina; ReactMarkdown+remarkGfm; CTAs `link`→navega rota Wouter registrada lesson #19, `tool`→confirm ADR-146; `degraded`→aviso "modo simplificado"); **categorias de nudge novas** `B-GAPCHECK` (gap-check D-3=sexta, check de estado **real** — nao importou+tem sessoes / sessoes sem reconciliar / snapshot pendente / 0min estudo+foco ativo / foco sem update de stats; nao cobra quem esta OK — risco R5; `gapCheckTick` Pro+ opt-in, `cycleKey=YYYY-WW`) e `B-IMPORT` (`bImportTick` Pro+, `lastImportAt` >`COACH_BIMPORT_DAYS`(5) + `sessionsInPeriod>0`, `cycleKey=YYYY-WW`, link `/upload`); ambas: toggle novo em `userCoachPreferences` (`nudge_b_gapcheck`/`nudge_b_import` `NOT NULL DEFAULT true`, migracao 0067), `updateCoachPreferencesSchema`+`unfreezeCategory`+`NudgeCategory`+`CATEGORY_TOGGLE_MAP`, passam por `shouldSendNudge`, auto-freeze cobre; ticks em `server/coach/jobs/`, `withAdvisoryLock`, gated `COACH_NUDGES_ENABLED` (Sprint AI-1B) | Aceito | 2026-05-12 |
| [158](158-quick-suggestions-anti-blank-page.md) | **Quick suggestions anti-blank-page — `GET /api/coach/suggestions`** (mapa estatico por rota + check leve de estado, cache TTL 30s `_resetSuggestionsCacheForTests`, fallback frontend estatico; **nao-LLM** — strings num modulo `server/coach/quickSuggestions.ts`, custo/latencia zero): `?route=<route>&...` → `{ suggestions: Array<{ id, text, sendOnClick }> }` 2-4 itens; variantes de estado (downswing→"por que estou perdendo?"; sem dados→"como importo?"); rota desconhecida→set generico 200 (nao 400); `requireAuth`+`injectedStorage?`; `ChatPanel`/`MiniChat` renderizam chips quando `messages.length===0`, clicar preenche o input (ou envia); endpoint falha→fallback estatico `client/src/lib/quickSuggestionsFallback.ts`. Substitui o conceito legado de "prompt starters" por-coach (`CoachAI.prompt-starters.test.tsx` `describe.skip`) (Sprint AI-1B) | Aceito | 2026-05-12 |
| [159](159-ai-1c-daily-debrief-monthly-report-tier-gating-hierarchical-summarization.md) | **AI-1C — Daily Debrief (event-driven) + Monthly Report (cron dia 1 7h tz) + tier gating estrito + sumarizacao hierarquica Haiku→Sonnet + generalizacao do gerador** (migracao 0068: `user_coach_preferences.report_daily_enabled` + `.report_monthly_enabled` `boolean NOT NULL DEFAULT false`; `report_type` continua varchar livre — sem ALTER): **Daily Debrief** enfileirado em `handleUpdateGrindSession` (PUT `/api/grind-sessions/:id` quando `status='completed'`) — best-effort fire-and-forget `void enqueueDailyDebriefForSession(...).catch(console.error)` (nunca quebra/atrasa a resposta do PUT), so insere `report_jobs` row `'daily'` (NAO gera sincrono — o processor 15min gera via `dailyDebriefGenerator`), `period_start` = data da sessao no fuso do user, cap 1/dia (UNIQUE — o gerador agrega as sessoes do dia), usa `session_tournaments` da sessao (NAO o historico), respeita `COACH_NUDGES_ENABLED`, ~$0.013; **Monthly Report** regra "dia 1 do mes 7h no fuso do user" no enqueuer hourly do AI-1B (`getLocalHour===7 && civilUtc.getUTCDate()===1`), `period_start` = 1o dia do mes anterior (`Date.UTC` — vira do ano OK), `monthlyReportGenerator` (sonnet 4.6 + sumarizacao Haiku se bundle grande), `content` v2: 5 secoes do weekly + comparativos vs mes -1/6m/12m (`buildMonthlyComparatives(12)` compondo `getPerformanceByPeriod` por mes — `getMonthlyPerformanceSeries` NAO criado) + variancia heuristica (`getVarianceVsExpected` retorna `null` hoje → `variance.method='heuristic'`, confidence baixo; `'primedope'` quando AI-2A) + leaks resolvidos/novos (`detectLeaks` x `coach_leak_focus` + `verify_leak_progress` — sem tabela historica) + progresso das metas (`AiStructuredProfileMeta` texto livre — LLM interpreta) + 3-5 insights + plano do proximo mes, ~$0.11; **tier gating estrito** `server/coach/reportEligibility.ts` — `getReportTier(user)` ∈ `{'free','eligible'}` (`eligible` p/ `subscription_plan='trial'` OU `resolveUserTier ∈ {pro,premium,admin}`; Free nunca; `resolveUserTier` NAO muda) + `isReportEligible(userId, type)` (tier eligible && opt-in do tipo; safe-deny), enqueuer itera `listUsersForCron("subscription_plan IN ('trial','active','admin')")` + filtra via `isReportEligible`, processor revalida (downgrade → `skipped`) — **corrige o bug latente do AI-1B** (`PRO_PLANS=['pro','premium','admin']` + `IN ('pro','premium')` so pegava `admin`); **sumarizacao hierarquica** `server/coach/hierarchicalSummarizer.ts` `summarizeBundleHierarchical({ bundle, reportType })` — camadas raw→mes→trimestre→ano para series longas (12 meses), threshold `COACH_REPORT_SUMMARIZE_THRESHOLD_CHARS` (~20k), Haiku via `COACH_REPORT_SUMMARIZER_MODEL ?? COACH_MEMORY_MODEL ?? haiku`, fail-soft = bundle truncado (`summarizer_model_used=null`, logado), daily nunca aciona, substitui o stub do AI-1B, reusa o pattern de `coachMemory.ts`; **generalizacao do gerador** helpers compartilhados em `server/services/reportGeneratorShared.ts` (`persistReport` parametrizado por `reportType`, `sanitizeHref`/`ALLOWED_HREFS`, `computeCost`, `callLlm` `new AnthropicCtor` try/catch lesson #5/#35, `resolveStorage`, `renderMarkdownBase`, `buildReportFollowUp`), `weeklyReportGenerator.ts` refatorado + `dailyDebriefGenerator.ts` + `monthlyReportGenerator.ts` reusam, `processReportJobsTick` despacha por `job.reportType` (`'quarterly'`/desconhecido → `skipped`); `ReportContent` v2 (`reportType: 'weekly'\|'monthly'\|'daily'` + campos opcionais `comparatives`/`variance`/`leaksDelta`/`goalsProgress`/`followUp`/`sessionSummary`, `schemaVersion: 2`, frontend tolera 1 e 2 — lesson #7); render `WeeklyReportView` → `ReportView` generico (rota `/coach-ai/relatorio/:id`, badge do tipo na timeline, nenhum endpoint novo); `runBackfills` roda no weekly + monthly, NAO no daily; fail-soft + custos identicos ao AI-1B (Sprint AI-1C) | Aceito | 2026-05-12 |
| [160](160-bulk-query-dimensions-tool-batching.md) | **`bulk_query_dimensions` — tool batching que generaliza `query_dimension`**: extrai a logica interna `runQueryDimension(input, ctx)` de `server/coachTools/handlers/queryDimension.ts` (corpo do `handler` sem o `safeParse` — o caller valida); `query_dimension` e `bulk_query_dimensions` ambos a usam (DRY — sem regressao); `server/coachTools/handlers/bulkQueryDimensions.ts` — input `z.object({ queries: z.array({ id?: string } & queryDimensionInputSchema).min(1).max(8) })` (acima de 8 → `validation_failed`, nao trunca), output `{ results: Array<R> }` na mesma ordem onde cada `R` = `{ id?, dimension, groupBy, rows, totalCount, period, note? }` (shape de `query_dimension`) ou `{ id?, ok:false, error:'validation_failed'\|'handler_error', ... }` por-item (spec ruim NAO derruba as outras — robusto, logado antes lesson #9); execucao **sequencial** (reads no `tournaments` — evita pressao no DB; melhor que N round-trips de tool use); `requiresConfirmation: false`, `auditLevel: 'log'` (mesmo de `query_dimension` — "nivel none" do roadmap = nao requer confirmacao), `gateByTier: ['pro','premium','admin']` (registry filtra — free nao ve); registrada em `server/coachTools/index.ts` via `safeRegister`; testes validam presença individual `getTool('bulk_query_dimensions')`, nunca length de `coachTools` (lesson #8); `description` incentiva o LLM a batchar (Sprint AI-1C) | Aceito | 2026-05-12 |
| [161](161-follow-ups-inteligentes-coach.md) | **Follow-ups inteligentes no Coach** — 3 frentes: (1) seçao "Seu acompanhamento" (`ReportContent.followUp`) nos 3 relatorios — `buildReportFollowUp(storage, userId)` (em `reportGeneratorShared.ts`): foco de leak ativo via `findActiveLeakFocusList` + status via a logica de `verify_leak_progress` (reusada como funcao, nao como tool do LLM — `findActiveLeakFocus`+`queryStatByKey`) + metas em progresso (`getAiStructuredProfile.metas` com `prazo in {'mes','trimestre'}`) + `focoDoMes`; **se nada ativo → `followUp` ausente / seçao omitida** (nao inventa — lesson #11); o LLM gera a `narrative` (1-2 frases fechando o loop); render `## Seu acompanhamento` no markdown + `ReportView` so quando presente; (2) bloco `## Follow-ups abertos` no contexto **DINAMICO** do chat (`coachContext.ts` `assembleContext` — padrao `systemParts.push`, reusa `findActiveLeakFocusList`/`getAiStructuredProfile` sem duplicar queries, atencao lesson #17; **vai no DINAMICO — NAO no STATIC, NAO quebra o cache** ADR-019); (3) 1 instrucao no `GRINDFY_AI_BASE` (bloco STATIC, prompt unico do agente — lesson #10): "quando houver um bloco '## Follow-ups abertos' no contexto e for natural na conversa, mencione brevemente o follow-up pendente e ofereca fechar o loop — sem ser repetitivo nem interromper o assunto do usuario"; conteudo **passivo** (nao e nudge — nao passa pelo `shouldSendNudge`, nao tem `cycleKey`/quiet hours; risco de "ser chato" mitigado — e seçao de um relatorio que o user ja optou em receber); reusa `coach_leak_focus` + `verify_leak_progress` + `AiStructuredProfileMeta` (AI-0A/AI-1A) — zero tabela/migracao nova (Sprint AI-1C) | Aceito | 2026-05-12 |
| [162](162-variance-kpi-primedope-cache-fallback-heuristic.md) | **Variance KPI — integracao PrimeDope-cache + fallback heuristico** (Sprint Variance-1): `getVarianceVsExpected` substitui stub `null` em `server/storage.ts:11664-11668` por implementacao real — agrega P&L USD 90d (FX cascade), lookup mais recente em `primedope_runs` 90d (zod-validated `data.{ev,stdDev}`), branch `primedope-cache` vs fallback heuristico `sigmaUsd = 1.5 * stddev(daily_pnl_usd)` quando ausente / shape quebrado; gate unico `sessionsCount < 20 → null` no storage; clamp `sigmaMultiple ∈ [-10, 10]` + sanitize NaN/Infinity → 0; status `lucky`/`normal`/`unlucky` por sigma threshold; RF-02 sanitize defensivo em `server/routes/home.ts:722-736` (literal parse + `Number.isFinite`); `expectedSource` discriminador alimenta RF-04 empty-state CTA; reusa `fxResolver` (ADR-163) (Sprint Variance-1) | Aceito | 2026-05-15 |
| [163](163-fx-cascade-shared-resolver-canonical.md) | **FX cascade compartilhada (`fxResolver`) como fonte canonica para Variance KPI** (Sprint Variance-1): `storage.getVarianceVsExpected` reusa `server/services/fxResolver.ts:88` `resolveExchangeRates(userId)` (ADR-061 + ADR-121 — cascade canonica `users.exchangeRates → wallets.exchangeRates → system_fx_rates → FALLBACK_CONSTANTS`); **NAO criar `fx.ts` novo** (spec original sugeria extrair de `primedopeIntegration.ts:122-157`, mas duplicaria — lesson #10); `primedopeIntegration.ts` continua usando helper local legacy (divida tecnica conhecida, fora de escopo — follow-up Sprint AI-2A); testes mockam `server/services/fxResolver` path canonico (lesson #28) (Sprint Variance-1) | Aceito | 2026-05-15 |
| [164](164-home-overview-cache-invalidation-mutations.md) | **Cache invalidation `home-overview` em mutations correlatas** (Sprint Variance-1, RF-03 + RF-07): hibrido server + client — server-side `invalidateHomeOverviewCache(userId)` (helper publico ja exportado em `home.ts:77` — lesson #21) chamado por (a) `handleUpdateGrindSession` quando `status='completed'` (mesmo handler que recebe gancho Daily Debrief AI-1C) e (b) `POST /upload` apos persistencia CSV; client-side `queryClient.invalidateQueries({ queryKey: ['/api/home/overview'] })` em `usePrimedopeSimulation.onSuccess` (apos `setQueryData` do primedope cache); TTL 30s natural vira backstop (mutations menores nao cobertas); padrao consistente com `invalidateFocusStatsCache` home-reform-4 item 7; janela stale = 0s para os 3 triggers principais (Sprint Variance-1) | Aceito | 2026-05-15 |
| [165](165-coach-write-tools-grade-study-strict-bulk-confirm-undo.md) | **Coach write tools de grade/estudo (AI-2A)** — `bulk_propose_grade` strict + cap 20 + transação em massa (`db.transaction` lesson #32) + undo em lote por `payload_after.createdIds`; `schedule_study_block` em `study_sessions_v2` `status='planned'` (Q-B locked); `create_study_theme` recusa nome duplicado com `theme_name_duplicate` (Q-C locked) + sync bidirecional com `user_focus_stats.linked_themes` (Sprint stats-themes-linking-1); `mark_off_day` idempotente via UNIQUE em `user_off_days` (Q-D locked); todas com `requiresConfirmation:true` (ADR-146) + `auditLevel:'persist'` + `coachToolRunner` (ADR-145); algoritmo de geração reusa `getTournamentSuggestions` (AI-0A — sem reimplementar scoring) + `listOffDaysForUser` + detect já-em-grade/time-overlap/wallet-below-threshold (Sprint AI-2A) | Aceito | 2026-05-20 |
| [166](166-coach-diagnostic-tools-ocr-bridge-variance-plateau-rake-ratio-pool.md) | **Coach diagnostic tools de AI-2A** — 5 read tools (`analyze_variance` reusa heurística do Monthly Report extraída para `server/coach/varianceAnalysis.ts` lesson #10 DRY + refactor do `monthlyReportGenerator.ts`; `diagnose_plateau` combina ROI flat + study minutes + leaks ativos + selection drift com thresholds env-configuráveis; `compute_grind_study_ratio` benchmark 5:1–10:1 + null handling quando `studyHours=0`; `calculate_effective_rake` agrupa por site + rakeback de `wallet_transactions.reason='rakeback'` ADR-039; `query_pool_intelligence` LIKE em `tournament_pattern` com fallback `not_seeded` Q-F locked seed 12 rows BR) + tool-bridge OCR (sinal `## Upload Recente` no page-context DINÂMICO ADR-019 + quick suggestion contextual ADR-158, sem tool nova — LLM usa `read_user_hud_stats` AI-0A); todas `requiresConfirmation:false`, `auditLevel:'log'` (Sprint AI-2A) | Aceito | 2026-05-20 |
| [167](167-coach-nudges-downswing-volume-grade-tool-eligibility-tier.md) | **Coach nudges B-DOWNSWING/B-VOLUME/B-GRADE + `isToolEligibleTier` (AI-2A)** — B-DOWNSWING drawdown ≥15% janela 7d (Q-H locked single signal, env `COACH_DOWNSWING_DRAWDOWN_PCT`+`COACH_DOWNSWING_WINDOW_DAYS`); B-VOLUME terça 11h local user projeção linear vs baseline 4w (Q-I locked, env `COACH_VOLUME_DROP_PCT`); B-GRADE domingo 18h local `planned<3` next week (Q-I locked, supersede plano canônico "sábado"); ticks hourly `0 * * * *` + `withAdvisoryLock` ADR-144 + `iterateUsersWithTimezone` ADR-087 + `shouldSendNudge` ADR-085 + gateados por `COACH_NUDGES_ENABLED` ADR-152; `isToolEligibleTier(user, tool)` em módulo dedicado `server/coach/toolEligibility.ts` (separado de `coachAccess.resolveUserTier` que NÃO muda — Q-E locked Trial recebe as 8 tools AI-2A; Free não; padrão simétrico a `getReportTier` AI-1C); schema novo migration 0070 (`user_off_days` UNIQUE `(user_id,off_date)` + `tournament_pool_intelligence` 12 rows seed BR Q-F locked) (Sprint AI-2A) | Aceito | 2026-05-20 |
| [168](168-career-goals-table-vs-jsonb-and-tools.md) | **Metas de carreira em tabela dedicada `career_goals` (AI-2B)** — Q-A locked tabela estruturada (não JSONB em `ai_structured_profile.metas`); `ai_structured_profile.metas` legacy mantida sem mudança (universos paralelos, sync opt-in unidirecional `career_goals → metas` resumida 1 frase com `origem='career_goal'`, NUNCA inverso); helper leitura unificada `getAllUserGoals(userId)`; cap 5 ativas por código (env `COACH_CAREER_GOALS_MAX_ACTIVE`); tool `define_career_goal` write/confirm v1 (ADR-146 — preview cap-exceed → "arquivar mais antiga?") + undo 30min; tool `evaluate_career_goal` read-only puro (FX→USD lesson #6, NÃO grava `progress_note`); endpoints HTTP CRUD `/api/coach/career-goals` (GET/POST/PATCH/DELETE — ownership 404-not-403); `CareerGoalsPanel` UI no /coach-ai aba Preferências; gating `isToolEligibleTier` Pro+/Trial (ADR-167); migration 0071 (Sprint AI-2B) | Aceito | 2026-05-20 |
| [169](169-quarterly-career-review-enqueuer-irpf-summary.md) | **Quarterly Career Review (AI-2B)** — Q-B locked enqueuer hourly estendido (jan/abr/jul/out dia 1 7h local user, sem cron dedicado, reusa `report_jobs`/`reports` pipeline AI-1B/1C — `report_type='quarterly'` já reservado AI-1C ADR-159 varchar(16) livre sem ALTER); `quarterlyReportGenerator.ts` reusa `reportGeneratorShared.ts` DRY (persistReport/sanitizeHref/computeCost/callLlm/resolveStorage); branch novo `isReportEligible(userId,'quarterly')` em `reportEligibility.ts` (`PREF_FIELD_BY_KIND` adiciona `quarterly:'reportQuarterlyEnabled'`); sumarização hierárquica Haiku quase sempre dispara (bundle trimestral > 20K chars, ADR-159 RF-07); `ReportContent` v3 com 14+ seções (5 novas: cgameSnapshot ADR-170, mentalHandHighlights top3 ADR-171, careerGoalsProgress via evaluate_career_goal interno ADR-168, **irpfSummary BR-only Q-C** via `fxCascade.getAveragePtaxForRange` PTAX médio trimestre + `wallet_tx.reason='rakeback'` ADR-039 + disclaimer Q-C "informativo não substitui contador", disclaimer regulatório obrigatório ADR-173); `schemaVersion: 3` lesson #7; tool ad-hoc `compute_irpf_summary` read tier-gated Pro+/Trial para reconciliação pontual fora do trimestre; Sonnet 4.6 ~$0.18/quarterly (~$0.72/year/user); fail-soft `new AnthropicCtor` try/catch lesson #5/#35; `COACH_NUDGES_ENABLED=false` desliga (Sprint AI-2B) | Aceito | 2026-05-20 |
| [170](170-cgame-inchworm-deterministic-heuristic.md) | **C-game / Inchworm visualization (AI-2B)** — Q-D + Q6-founder locked heurística determinística zero LLM zero prompt invasivo; deriva A/B/C de `warmup_rituals` (em prod desde W-3): `emotional_check_score` + `override_used` + `decision_to_play` (NÃO adiciona coluna nova); thresholds env-config `CGAME_A_THRESHOLD` (default 8) + `CGAME_C_THRESHOLD` (default 4) — founder valida em piloto; `aggregateCgameForPeriod` + `getInchwormSeries(months=6)` + `getCgameMovement` em `server/services/cgameAggregator.ts` (puro, read-only, sem side-effect); confidence: `<8` low / `<20` medium / `>=20` high; endpoint `GET /api/coach/cgame/snapshot?period=30d|90d|trimestre|ano` `requireAuth` **NÃO tier-gated** (free vê seus dados próprios — engajamento); UI tab "Mental" no /coach-ai com `CgameSummaryCard` + `InchwormChart` Recharts area chart 3 séries; empty state quando sampleSize=0 link `/grind-live` lesson #19; movement narrative determinística (template, sem LLM); Quarterly Report consome `cgameSnapshot` em ReportContent v3 (Sprint AI-2B) | Aceito | 2026-05-20 |
| [171](171-mental-hand-history-tendler-framework.md) | **Mental Hand History framework Tendler (AI-2B)** — Q-E locked tabela dedicada `mental_hand_history` (não JSONB em `warmup_rituals.sessionIntention` — queryability + overload semântico); schema `(id, user_id, occurred_at, situation, emotion CHECK enum 'frustration|tilt|fear|overconfidence|fatigue|other', real_response, ideal_response, tags text[], linked_grind_session_id FK ON DELETE SET NULL preserva registro mental, created_at)` + indexes `(user_id, occurred_at DESC)` + `(user_id, emotion)`; tool LLM `log_mental_hand` write/confirm `requiresConfirmation:true` ADR-146 Pro+/Trial via `isToolEligibleTier` ADR-167; endpoints HTTP `GET/POST/DELETE /api/coach/mental-hands` abertos (free pode usar POST via UI form — rate limit 20/dia anti-abuse; tool LLM gated); UI viewer `MentalHandHistoryList` + `MentalHandForm` na tab "Mental" do /coach-ai (mesma do RF-05); `selectTopHighlights(rituals, 3)` em `mentalHandsSelector.ts` prioriza diversidade de emoção + recência — Quarterly Report consome em `ReportContent.mentalHandHighlights`; privacidade: texto sensível **nunca** para 3rd parties EXCETO Anthropic via Quarterly top 3 (não tudo); DELETE 404-not-403 ownership (Sprint AI-2B) | Aceito | 2026-05-20 |
| [172](172-email-pipeline-gmail-smtp-phase1-unsubscribe-hmac.md) | **Email pipeline relatórios Gmail SMTP fase 1 + unsubscribe HMAC (AI-2B)** — Q-F locked reuso `server/emailService.ts` + nodemailer + Gmail SMTP existente em prod (verification/welcome/reset) — sem SES/SendGrid agora, sem dep nova, rate limit ~500/dia aceito em alpha externo (follow-up SES quando volume justificar); Q-G locked **só Weekly/Monthly/Quarterly** — Daily Debrief **NÃO envia email** (high volume + low ROI + ruído); `server/services/reportEmailSender.ts` `sendReportEmail({reportId, userId, kind})` chamado **inline pelo processReportJobsTick** após report `status='ready'` (best-effort fire-and-forget `void ...catch(log)` — NÃO bloqueia tick); idempotência via `email_log` UNIQUE parcial `(report_id, kind) WHERE report_id IS NOT NULL`; retry exponencial 15min/1h/4h herdado de `reportJobRunner` AI-1B, após 3 tentativas `status='failed'` final; 3 templates HTML separados em `server/emails/templates/{weekly,monthly,quarterly}ReportEmail.ts` (subject/html/text — disclaimer regulatório no footer ADR-173 + escape HTML em texto livre do user lesson #16); unsubscribe link em todo email (compliance CAN-SPAM/LGPD) com HMAC SHA256 token `${userId}.${kind}.${expiresAt}.${hmac}` URL-safe (secret `EMAIL_UNSUBSCRIBE_SECRET ?? JWT_SECRET`, expires 1 ano); endpoint público `GET /api/coach/email/unsubscribe?token=...` (sem auth — HMAC valida via `crypto.timingSafeEqual`) desliga `email_<kind>_enabled` pref; `COACH_NUDGES_ENABLED=false` desliga TODO pipeline (Sprint AI-2B) | Aceito | 2026-05-20 |
| [173](173-disclaimer-regulatorio-3-superficies-texto-canonico.md) | **Disclaimer regulatório 3 superfícies + texto canônico (AI-2B)** — Q-H locked texto canônico único em `server/coach/disclaimers.ts` exportando `REPORT_DISCLAIMER` + `REPORT_DISCLAIMER_SHORT` + `DEFLECTION_RULE_REGULATORY` (IRPF/tax/regulamentação/staking/contrato/lei → deflete "consulte profissional especializado") + `FINANCIAL_CAVEAT_RULE` (qualquer output mencionando $/BRL/banca/profit/ROI/retorno como projeção → "Conteúdo informativo — não garante retorno futuro"); (1) **footer fixo `ReportContent.disclaimer`** populado em **todos** os 4 tipos de report (weekly/daily/monthly/quarterly) renderizado pelo `ReportView` como bloco final destacado + email templates ADR-172; (2) **bloco STATIC do `GRINDFY_AI_BASE`** com 2 regras (deflexão + caveat condicional) — cache `ephemeral` mantido válido ADR-019; (3) **`OnboardingWizard` AI-1A** ganha step penúltimo com checkbox aceite explícito → POST `/api/coach/onboarding/accept-disclaimer` → UPDATE `user_coach_preferences.disclaimer_accepted_at = NOW()`; back-fill banner não-bloqueante no `/coach-ai` para users pré-AI-2B (`disclaimer_accepted_at IS NULL`) — single-click aceite, não interrompe uso (lesson #11 default mínimo); auditoria via timestamp serve proof of consent; PT-BR fixo; Termos de uso completos `/terms`/`/privacy` fora de escopo (disclaimer NÃO substitui ToS) (Sprint AI-2B) | Aceito | 2026-05-20 |
| [174](174-ai-3-cleanup-wiring.md) | **AI-3 cleanup wiring** — Quarterly LLM real-call Sonnet 4.6 (paridade `monthlyReportGenerator` + `tryAnthropicClient` lessons #5/#35); FX cascade delega `bcbPtaxAdapter` + `frankfurterAdapter` reais (shape `FxRow[]`); `cgameRecent` snapshot persist best-effort em `users.ai_structured_profile` JSONB schemaless via `updateCgameRecent` (Q-D trigger só no quarterly); `enqueueQuarterlyReportJobsTick` migra `iterateUsersWithTimezone` → `listUsersForCron("subscription_plan IN ('trial','active','admin')")` paridade weekly/monthly; DRY `shared/brTimezones.ts` + `_renderReportShell.ts` (quarterly-only — Q-A locked `shared/fxCascade.ts` em `shared/`); perf `Promise.allSettled` 2 waves quarterly gather + `getInchwormSeries` paralelizado. Q-B/Q-F locked. (Sprint AI-3) | Aceito | 2026-05-20 |
| [175](175-calendar-days-since-dst-aware.md) | **`calendarDaysSince` DST-aware** — util canônico `shared/calendarDaysSince.ts` substitui 4 implementações locais (`WalletStalenessBadge`, `dailyInsight`, `Dashboard` inline, `mentalPrepUtils`). Algoritmo: extrai `YYYY-MM-DD` de from/to no `timeZone` do user via `Intl.DateTimeFormat('en-CA')`, converte para `Date.UTC(y,m-1,d)` e retorna diff em dias civis. Elimina off-by-one em transições DST. (Sprint UX-QW-3 RF-03) | Aceito | 2026-05-21 |
| [177](177-study-session-dedicated-page.md) | **Pagina dedicada de sessao de estudo + linkagem dual de notes (Estudos-Sessao-1)** — Opcao A: nova coluna `study_sessions.status varchar(16) NOT NULL DEFAULT 'active'` (enum `active\|finished\|abandoned`) + index parcial `(user_id) WHERE status='active'`; nova coluna `study_notes.study_session_id varchar(21) REFERENCES study_sessions(id) ON DELETE CASCADE` nullable + `study_card_id` vira NULLABLE (deprecation gradual lesson #7) + CHECK XOR-fraco `(study_card_id IS NOT NULL) OR (study_session_id IS NOT NULL)`. Pagina nova `/estudos/sessao/:id` no shell `Studies.tsx` (sem rota nova em `App.tsx`). Timer derivado de `session.date` (startedAt) — sobrevive a refresh F5 (lesson #12). Migration 0073. Veta opcao B (linkar a `themeId`, perde granularidade) e C (StudyCard virtual, confusao semantica + dirty data). (Sprint Estudos-Sessao-1) | Aceito | 2026-05-21 |
| [178](178-bankroll-tristate-mode.md) | **Bankroll tristate semantics (TS-3 RF-04)** — Filtro `bankrollFilter` boolean → tristate `bankrollMode ∈ {all,hide,warn}` persistido em `user_settings.tournament_selector_bankroll_mode VARCHAR(8) NOT NULL DEFAULT 'warn' CHECK IN ('all','hide','warn')` (migration 0072); payload `bankrollWarning: {reason: 'above_hard_limit'\|'above_soft_limit', limitUSD, buyInUSD, rulePct}` quando `mode='warn'` (Q-I locked: `bankrollRules.ts` so tem hard/soft hoje — shape extensible para `above_<rule>` no futuro sem CHECK constraint na coluna pois coluna so armazena mode); alias `bankrollFilter` deprecated (`true→hide`, `false→all`); cache-key extendido `(userId,date,sources,bankrollMode)`; telemetria `tournament_selector_logs.metadata.bankrollMode` em todo `view`+`add_to_grid`; default `warn` beneficiario principal cohort 2 mid-grinder. (Sprint TS-3) | Aceito | 2026-05-21 |
| [179](179-tournament-selector-calibration-dashboard.md) | **Calibration dashboard + admin gate + a11y exception (TS-3 RF-05)** — `GET /api/admin/tournament-selector/calibration?lookbackDays=N` `requireAuth + requirePermission('admin')` agrega correlacao grade → ROI realizado via JOIN `tournament_selector_logs(event_type='add_to_grid') ↔ tournaments(grind_session_id IS NULL §6.1)` matching dual (externalId forte + heuristica datetime 14d/48h fallback para library sem externalId); `expectedRoiPct` heuristico mid-point band (S=21.25, A=13.5, B=6, C=-1.5, D=-15); `discrepancyPct = realizedRoiPct - expectedRoiPct`; warnings `bucket_off_calibration_<grade>` quando `\|discrepancy\|>5pp AND adds>=20` + `sample_low_in_grade_<grade>` quando realized<5; **`insufficientData: true` flag quando `totalAdds<50`** (Q-B locked — UI pronta hoje, valor total quando volume chegar); cache TTL 1h server-side `Map<lookbackDays>`; **a11y exception documentada** (Q-N locked — semantic table + sr-only labels apenas, sem axe-core obrigatorio na rota admin de 1-3 users); LGPD ok (agregacao por grade nao expoe userId). Zero migration. (Sprint TS-3) | Aceito | 2026-05-21 |
| [184](184-tickets-cron-expiracao-e-notifs.md) | **Tickets cron expiracao + notifs in-app (Sprint D RF-03.1+RF-03.2)** — `server/jobs/expireTickets.ts` housekeeping diario `0 3 * * *` UTC, SEM gate `COACH_NUDGES_ENABLED` (Q-A locked — expiracao eh estado, nao proatividade); UPDATE `tickets SET status='expired'` idempotente com guard `AND status='available'`; INSERT `notifications` `ticket_expiring` (<=48h) + `ticket_expired` com dedupe 7d via `deep_link LIKE '%ticket_id=...'`; reuso `cronRunner` + `withAdvisoryLock("cron:expire-tickets")` + tabelas existentes (zero migration); telemetria `user_activity event_type='ticket_expired'`; race protection (re-confirm SELECT pre-INSERT). | Aceito | 2026-05-21 |
| [185](185-tickets-coach-context-block-keyword-triggered.md) | **Coach AI context block tickets (Sprint D RF-03.3)** — `server/coach/contextBuilders/buildTicketsContext.ts` retorna `TicketsContextBlock \| null`; injecao CONDICIONAL no bloco DINAMICO (sem cache ephemeral — risco stale quando user usa ticket via tool); gate dual (a) `pageContext.surface ∈ {tournament-selector,grade-planner}` OU (b) 4 keywords spec `ticket\|satelite\|grade\|selecionar torneio` (NFKD normalizado lower-case); format markdown compacto (total + ate 3 proximos a expirar sort asc); `null` quando 0 tickets ou nao-trigger (early return preserva tokens — lesson #11); reuso `storage.getActiveTicketsByUser` + `coachSystemBuilder.DynamicInputs.ticketsContext`. Sem tool dedicada (YAGNI). | Aceito | 2026-05-21 |
| [186](186-selector-ticket-boost-availabletkt-opcional-bankroll-bypass.md) | **Tournament Selector ticket boost (Sprint D RF-03.4)** — Campo NOVO opcional `availableTicket?: {id,valueUsd,expiresAt}` em `ScoringInputTournament` (`shared/scoring.ts`) — ownership `server/scoring/buildScoringInput.ts` (AI-0A territory; PROIBIDO mexer `server/services/scoring*` Sprint B); helper `enrichWithTickets(sct, userTickets, tournament)` com match heuristico (exact sourceName -> value tolerancia 1% -> FIFO menor `expiresAt`); boost `+10 score` aplicado FORA de `computeTournamentScore` no caller (`Math.min(100, baseScore + 10)` + re-derive `scoreToGrade`); bankroll filter bypass (`availableTicket != null` trumpa `effectiveBuyIn > maxBuyIn` pq ticket nao consome banca); UI badge `🎟️ Ticket disponivel ($X)` em `SelectorPanel` (lesson #28: edit direto, grep import antes); telemetria `selector_item_shown_with_ticket_boost`. Falso positivo heuristica aceitavel (user ve valueUsd, decide). | Aceito | 2026-05-21 |
| [187](187-audio-source-engine.md) | **AudioSourceEngine abstraction + driver pattern (Mini Player 1 RF-06)** — Interface `IAudioSourceDriver` (load/play/pause/seek/setVolume/setSpeed/getCurrentTime/getDuration/destroy + on('timeupdate'\|'ended'\|'durationchange'\|'error')) + discriminated union `AudioTrack` com `source: 'library' \| 'spotify'` + facade thin `AudioSourceEngine` (singleton dentro do Provider) que delega ao driver baseado em `track.source`. Fase 1 implementa apenas `LibraryAudioDriver` (wrap `HTMLAudioElement`); `SpotifyAudioDriver` fica STUB que lanca erro ate Sprint Mini Player 2 (Fase 4 Spotify Web Playback SDK lazy import ~30KB). Isola contexto React (volume scroll wheel, displayMode, glassmorphism) do runtime de audio (HTML5 vs Spotify SDK). Backward compat preservada via wrapper `play(lesson)` → `playTrack({source:'library',...})`. Testes mockam interface sem polyfill `HTMLAudioElement`. Precedente interno: `server/services/fx/adapters/{bcbPtaxAdapter,frankfurterAdapter}` (ADR-163 / AI-3). | Aceito | 2026-05-21 |
| [188](188-mini-player-displaymode-fsm.md) | **Mini Player displayMode FSM + z-index hierarchy (Mini Player 1 RF-04 + RF-11)** — FSM canonica `'hidden' \| 'bar' \| 'expanded'` em union literal TS + pure transition functions (`canExpand`/`nextOnEsc`/`nextOnClose`/`nextOnPlayTrack`/`nextOnFullscreenEnter`/`nextOnFullscreenExit`) em `client/src/lib/audio-engine/displayModeFsm.ts`. Estados invalidos prevenidos compile-time (TS narrowing) + runtime (`setDisplayMode('expanded')` com `current===null` no-op + console.warn). Esc contextual (expanded → bar → hidden) em 1 funcao pura testavel. Sem persist cross-session (D4 + D7 + D8: refresh = hidden). **Z-index map empirico:** MiniPlayerBar z-40, MiniPlayerExpanded z-45, MiniChat z-50 (verificado em `client/src/components/MiniChat.tsx:172,196`), Radix Dialog z-[100], Sonner z-[9999]. Gap `z-40 → z-45 → z-50` reserva `z-41..z-44` pra floating icon mode (fase futura Q4). Body padding-bottom condicional via CSS variable `--mini-player-height` (0/64/72/80px conforme `displayMode` + breakpoint) — layouts opt-in granulares (Dashboard/GradePlanner/GrindSessionLive/Biblioteca), nao body global. Rejeitadas: booleans separados (permite estado nonsense `{visible:false,expanded:true}`), Zustand/Jotai (overkill, viola convencao TanStack+Context), XState (~30KB > RNF-01 8KB). Media Session API (D12/D17) NAO ganha ADR proprio (~20 linhas dentro do contexto; reabrir se Spotify exigir handlers diferentes). | Aceito | 2026-05-21 |
| [181](181-csv-parser-prize-kill-switch-and-bulk-progress.md) | **csvParser prize semantics kill-switch + bulk-import progress (Backend-Sweep RF-01/RF-02)** — Env flag `CSV_PARSER_NEW_PRIZE_SEMANTICS` (default `false`) ativa lookup `PRIZE_FORMULA_BY_NETWORK` para canonical `prize=NET` por rede + log audit `csvParser.new_prize_semantics.applied` com breakdown por rede pra diff manual antes do backfill historico (~200K rows). Bulk-import async: migration 0074 adiciona `upload_history.processed_count integer NOT NULL DEFAULT 0` + `idx_upload_history_status`; POST `/api/upload-history` branch sync ≤5K vs async >5K (background batches BATCH_CHUNK=500 + UPDATE `processed_count` por batch, status `'success'\|'failed'\|'processing'`, partial-failure preserva trabalho); novo GET `/api/upload-history/:id` polling 2s. Cache invalidation (playerBundleCache + selectorCache) gated por successCount>0. (Sprint Backend Tech-Debt Sweep) | Aceito | 2026-05-21 |
| [209](209-mini-player-modern-redesign.md) | **Mini Player modern redesign (Sprint MP-MODERN)** — Redesign visual + UX polish sem mudar arquitetura. 6 RFs: RF-01 hero cover responsive h-12/14/16 + custom keyframes `pulseSubtle` (Tailwind, sem Framer Motion runtime) + data-cover-state attribute (anti-Tailwind-raw-class test contract); RF-02 progress bar gradient blue→purple + thumb h-3 touch-target + mm:ss font-mono + scrub tooltip mouseMove; RF-03 controls pill `bg-white/5 rounded-full` + toggle bg-white h-10 (Spotify-like affordance) + outros h-8; RF-04 sidebar 3 dividers aria-hidden (grupos audio/queue/utils + janela); RF-05 ExpandedPlayerDialog NOVO (Radix Dialog full-screen + hero max-w-md + transcript + queue inline + coach hint dismissable `coach_hint.expanded.seen.v1` localStorage + Wouter Link `/coach-ai` + ErrorBoundary local lesson #29 + 5 close reasons telemetria); RF-06 EmptyStateCTA NOVO (renderiza quando !activeTrack: "Escolher aula" + "Conectar Spotify" — remove early return MiniPlayerBar:173 + default displayMode flip `hidden`→`bar`). Telemetria namespace `mini_player.*` (5 eventos novos). Lessons #1 hooks order (early return só após hooks), #22 tokens shape (cores via data-attrs, NÃO Tailwind raw), #23 Wouter v3 Link safe, #29 ErrorBoundary local em transcript sub-arvore + safe wrappers `useSafeSpotifyStatus`/`useSafeAuth`, #38 ESM vs CJS via setup.ts harness fix (`new Function` wrapper bypass Node 24 type:module ESM gate + vi.mock-aware require shim). Build size +<10KB gzip cap. ExpandedPlayerDialog co-located em MiniPlayerBar (não App.tsx). Tailwind config: `pulseSubtle` keyframes 95↔100% opacity 2s alternate; index.css `.mini-player-range` gradient + accent-color #6366f1 fallback iOS Safari. Reduced-motion respeitado (data-reduced-motion). Tier gate: Spotify connect botão somente em activeSource !== "spotify". Sem migration, sem schema. | Aceito | 2026-05-23 |
| [180](180-coach-tool-tournament-selector-consolidation.md) | **Coach tool tournament selector consolidation (TS-3 RF-02 Q-G)** — `get_tournament_suggestions` existente (AI-0A) **estendida** em vez de criar `tournament_selector_recommend` nova (Q-G locked apos leitura: handler atual ja usa `tournamentScoringService.rankTournamentsForContext` → `computeTournamentScore`, output ja tem score/grade/confidence/rationale; tool nova duplicaria 95%); +`source ∈ {suprema,library,both}` +`bankrollMode` (herda `user_settings` ADR-178) +`topN` alias no input; +`bankrollWarning` +`alreadyInGrid:bool` +`source` explicit no output; **cache compartilhado** com `/api/tournament-selector` via modulo extraido `server/scoring/tournamentSelectorCache.ts` (chave `(userId,date,sources,bankrollMode)` TTL 30min — cross-surface hit garantido entre widget e Coach); **telemetria dual** `coach_tool_invocations` (runner ADR-145) + `tournament_selector_logs` com `metadata.invokedBy='coach_tool'` (Q-L locked: enum `['widget','coach_tool','admin_dashboard']` em JSDoc sem CHECK constraint); tier gate via `isToolEligibleTier` (ADR-167 AI-2A — Trial passa, substitui `gateByTier: ['pro','premium','admin']` literal); **tool eh data retrieval — sem custo LLM extra** (Q-M locked: LLM gera narrativa no turno normal). `explainTournamentScore` confirmado ortogonal. (Sprint TS-3) | Aceito | 2026-05-21 |
| [213](213-day-detail-zoom-consolidation.md) | **DayDetailZoom Consolidation (Sprint day-detail-consolidation)** — Formaliza 8 decisoes arquiteturais (D1-D8) que sustentam as 9 RFs ja shippadas em 8 commits founder-direto (`2a2a514c..439bb60c`) + 4 fixes de bugs latentes + 3 sanitizacoes de smells. D1 bucketing `HH:00` via `maxLate ?? time` (display preserva raw). D2 reuse coluna legacy `registration_time` como `maxLate` (zero migration; rename adiado para Sprint dedicado 2-step). D3 optimistic priority overlay `Record<id, priority>` + cleanup via `useEffect` quando server confirma (short-circuit `changed?next:prev` evita re-render — RF-SMELL-03 fix). D4 click-away custom via `data-zoom-menu` markers + `document.mousedown` listener gated por `openMenuId`. D5 stacking context 3-layer: overlay `z-50` (portal raiz) + chip `z-40` + menu `z-50` (dentro `#dnd-portal-container` — stacking separado). D6 default biblioteca `libraryCollapsed=true` + Panel imperative `.collapse()` deferred via `setTimeout(0)` apos paint. D7 debounce telemetria movida para `useEffect` (corrige antipadrao linha 108 `BibliotecaEmbedded` — quebra com `vi.useFakeTimers`). D8 hash-based `siteColors` fallback determinista (charCodeAt sum mod 6 paletas) para sites nao mapeados. Estende ADR-210 (modal base) — NAO substitui. Zero migration, zero endpoint novo. (Sprint day-detail-consolidation) | Aceito | 2026-05-28 |
| [220](220-spotify-e2e-playback-csp-sdk.md) | **Playback Spotify client-side via Web Playback SDK + CSP allowlist Spotify + start-on-device (Sprint Spotify E2E)** — Destrava o playback (gating absoluto: a CSP Helmet em `server/routes/index.ts` bloqueava `sdk.scdn.co` → `window.Spotify` nunca existia). D1 playback permanece client-only (proxy server-side rejeitado — SDK e client-only por design/DRM). D2 CSP ampliada SO com hosts Spotify/SCDN explicitos (sem curinga `https:`; `'unsafe-eval'` NAO em prod): `scriptSrc+=sdk.scdn.co`; `connectSrc+=api.spotify.com,*.spotify.com,wss://dealer.spotify.com,wss://*.spotify.com,*.scdn.co`; `frameSrc+=sdk.scdn.co,*.spotify.com`; `mediaSrc+=*.scdn.co,*.spotify.com,sdk.scdn.co`; `styleSrc+=fonts.googleapis.com`; `fontSrc+=fonts.gstatic.com` (RF-05). `imgSrc` inalterado (`https:` cobre capas); `workerSrc` mantido `[self,blob:]` ate browser provar. D3 start direto `PUT /me/player/play?device_id=<ready.device_id>` { uris } (sem transfer separado). D4 lifecycle: gesto→activateElement (EME/autoplay)→ready(device_id)→play; deviceId-ready queue (play antes do ready enfileira, NAO no-op). D5 controle de velocidade OCULTO quando `activeSource==='spotify'`. D6 RF-07 `requireSpotifyAccess`: TypeError de dep ausente NAO marca disconnect (so corrupcao real do ciphertext). D7 eventos telemetria playback no allowlist ADR-207. Relates ADR-190/194/207/208. Pendencias follow-up: FK `user_subscriptions.user_id` em prod + transfer condicional + saida do Dev Mode Spotify. Sem migration, sem endpoint novo. | Aceito | 2026-05-31 |
| [221](221-spotify-polish-design-decisions.md) | **Decisoes de design do Sprint Spotify Polish (autoplay/fila/resume/token/cover/status) — 8 bugs nao-triviais** — Cobre so os bugs do manifesto `sprint-spotify-polish-bugs.md` que exigem decisao de design; os mecanicos vao direto pro implementer. **D1 (B-ENDED-1/RF-09)** FSM em `onStateChanged` com `lastState` (lastPositionMs/wasPlaying/lastTrackUri) + dedupe `endedEmittedForUri` — emite `ended` 1x/faixa via OR de 3 sinais (C1 playing→reset-pos-0 perto do fim, C2 migracao p/ `previous_tracks`, C3 fallback legado pos>=dur); reset em load/play. **D2 (B-QUEUE-1/RF-08)** invariante replace-vs-append: Play (avulso busca/Tocar tudo/faixa drill-in) = `clearQueue()` antes; Adicionar (+/Adicionar tudo) = anexa; `clearQueue` ja exposto (sem mudanca de contrato no context); drill-in row vira clicavel (clear + playTrack(i) + enqueue i+1..n). **D3 (B-RESUME-1)** split `IAudioSourceDriver.resume()` (SDK `player.resume()` / PUT play sem body) vs `play()` (load com uris) — `useEffect[isPlaying]` chama resume/pause, nao play; library driver resume=alias. **D4 (B-AUTOPLAY-1)** invariante `playTrack`: `setCourseContext(ctxArg ?? null)` + `courseContextRef.current=ctxArg ?? null` SEMPRE sincrono — autoplay Spotify cai na fila, nao na aula residual. **D5 (B-TOKEN-CLOSURE/B-VOLUME-1)** factory le `spotifyTokenRef.current` na construcao (nao closure — anti 401-churn pos-refresh) + aplica `volumeRef`/`isMutedRef` ao driver pos-construcao. **D6 (B-COVER-1)** SSoT `shared/spotifyCoverHosts.ts` allowlist por SUFIXO `.scdn.co`/`.spotifycdn.com` (boundary-safe) consumido por server+client (capas custom playlist/album param de virar placeholder cinza; alinhar CSP img-src). **D7 (B-BOOT-1/B-DISCONNECT-SYNC/B-PANEL-401)** fetcher unico resiliente `fetchSpotifyStatus` (401≠logout) p/ hook+painel (mesma queryKey); `disconnectSpotify` invalida status no helper; bootstrap invalida quando `resolveViaStatusFallback` null (UI nunca afirma conectado com driver null). **D8 (B-ARTIST-1/RF-03)** `AudioTrack.artist?`/`AudioTrackLike.artist?` opcional (lesson #7); callsites passam `artists.join(", ")`; render `artist ?? courseTitle ?? ""`. RF-07 preserva `fetch.bind(globalThis)`/`tryAutoplayNext`/cap-50/tier-`active`/CSP. 4 diagramas em `diagrams/spotify-polish/`. Sem migration. | Proposto | 2026-05-31 |
| [211](211-variance-native-monte-carlo-engine.md) | **Substituir dependencia externa PrimeDope por engine Monte Carlo nativo (Sprint VR-1)** — Engine nativo `server/services/varianceEngine.ts` substitui fetch externo a `primedope.com` (nunca funcionou em producao — 4xx/5xx consistente). Algoritmo: payout power-law (alpha variavel por field size + PKO flattening) + skill factor calibrado via binary search 200 iter + Monte Carlo 10K sims default. Remove ~300 linhas dead code (Semaphore, fetchPrimedopeOnce/WithRetry, saveChartFromUrl, getChartFsPath, rate limit 1/10s, chart proxy route). Endpoint canonico `POST /api/variance/simulate` + alias backward-compat `/api/primedope/simulate`. Cache 5min (reduzido de 30min — simulacao local ~0.3s). Output: EV, stdDev, profitablePct, 9 percentis, drawdown 5 stats, histograma, groupContributions. Seed opcional para reprodutibilidade. PrimedopeResult.tsx: RoR card substituido por "Chance de Lucro" (profitablePct), minBankroll substituido por drawdown card, CI table usa percentiles nativos, risk badge por profitablePct thresholds. Supersedes ADR-054. Sem migration (source varchar livre). | Aceito | 2026-05-25 |
| [223](223-coach-weekly-triple-delivery-enablement.md) | **Entrega tripla idempotente + enablement default-ON dos relatórios do Coach (Sprint EST-1)** — _(renumerado 222→223: ADR-222 ficou com Sprint EST-3 stat_analysis, sessão paralela)_ — Faz o relatório Weekly/Daily/Monthly chegar ao jogador. **D1** RF-01 mapeia as 5 colunas AI-2B da migração 0071 no drizzle `userCoachPreferences` (drift que zerava `prefs.emailWeeklyEnabled`→`undefined`→email `skipped`); defaults drizzle = `false` (estado pré-0086; o flip é da migração); `disclaimerAcceptedAt` fora do zod `.strict()` (spoofável). **D2** Migration **0086** (confirmado: 0085 é o maior): `ALTER COLUMN ... SET DEFAULT true` em 5 cols delivery (report_weekly/daily/monthly + email_weekly/monthly; quarterly intocado) + back-fill `WHERE subscription_plan IN ('trial','active','admin')` (= LIST_USERS_FOR_CRON_PRO_PLUS); rollback reverte defaults + DROP ledger, NÃO desfaz back-fill (irreversível por design D2/D6). **D3** Ledger = Opção A `reports.delivered_channels` jsonb (sobre tabela `report_deliveries`); claim atômico `markReportDelivered` UPDATE condicional `WHERE NOT (delivered_channels ? channel)` RETURNING (anti-race row-lock; lesson #33); email mantém barreira própria `email_log` UNIQUE — chave email no jsonb é só observabilidade. **D4** Chat post em `coachType='technical'` (enum canônico = mental/tournament/technical, sem 'main'); `getOrCreateReportChatSession` (método novo — `getChatSessions` não existe) reusa sessão technical ativa+recente; conteúdo determinístico PT-BR por tipo, SEM LLM. **D5** Módulo dedicado `server/services/reportDelivery.ts` `deliverReport({reportId,userId,reportType}, injectedStorage)` (lesson #34), ordem in-app→chat→email, cada canal try/catch isolado, log antes do fallback (lesson #9); ponto de chamada em `processOneJob` (~L610) padrão `void ...catch(log)`. **D6** Entrega uniforme p/ weekly/daily/monthly (todos: in-app+chat; email só weekly/monthly — daily sem email Q-G); quarterly fora de EST-1. Sem endpoint HTTP novo. 2 diagramas em `diagrams/coach-est-1/`. | Aceito | 2026-06-01 |
| [224](224-est6-next-week-planning-flow.md) | **Next-Week Planning Flow (EST-6) — orquestrador backend + estado leve + UI conversacional no chat** — O mentor conduz o jogador a montar o plano da próxima semana em 4 sub-decisões (grind→study→lessons→themes), standalone e pluggável pelo EST-5. **DEC-1** orquestrador `server/coach/planning/weeklyPlanningOrchestrator.ts` (lista ordenada de passos, NÃO state machine) + estado leve em `weekly_planning_sessions` + wizard embutido no chat `/coach-ai` (NÃO página Wouter — lesson #19). **DEC-2** mapa `:step`→tool AI-2A: grind→`bulkProposeGrade` (fetchPayloadBefore/executeConfirmed), study→loop `scheduleStudyBlock`+`upsertStudyWeeklyPlan`, lessons→`recommendLesson` (whitelist) + `createCoachRecommendation` (o tool NÃO persiste rec — orquestrador grava), themes→read-only `getStatsLeaks`; `mark_off_day` é auxiliar do grind. **DEC-3** idempotência: UNIQUE `(user_id,week_start_date)` + UPSERT `study_weekly_plans` (UTC) + delete-then-insert `coach_lesson_recommendations` (BRT). **DEC-4** gate `getReportTier!=='free'` na entrada + cada tool revalida `gateByTier` (defense in depth). **DEC-5** migration **0088** `weekly_planning_sessions`. **Aberta #1** re-propor: grind=dedup `already_in_grade` (undo só em "refazer"), study=UPSERT+só blocos novos, lessons=delete-then-insert. **Aberta #2** colisão de rota descartada (sem catch-all `/api/coach/:x`; prefixo `planning` disjunto) — novo `server/routes/coachPlanning.ts` registrado ANTES de `registerCoachAi1bRoutes` + guard test `est-6-route-collision.test.ts`. **Aberta #3** resumo RF-06 reusa `getOrCreateReportChatSession`+`insertChatMessage` (role=assistant) do EST-1, NÃO `deliverReport`. **Aberta #4** shape `steps` jsonb + Zod em `shared/coach-planning.ts`. **Aberta #5** `source` enum `coach_manual\|est5_ritual` (EST-5 passa flag). **Chaves**: `ymdUtc` extraído p/ `server/coach/planning/weekKeys.ts` (UTC) + `brtMondayYmd` (BRT). 2 diagramas em `diagrams/coach-est6/`. Migration 0088 PENDENTE PROD. | Aceito | 2026-06-01 |
| [225](225-est2-weekly-report-data-enrichment.md) | **Weekly Report Data Enrichment (EST-2) — mentor lê break_feedbacks + grind notes + métricas de estudo EST-3** — O Weekly Report passa a interpretar qualitativamente o estado mental e de estudo da semana. **D-1 custo** NÃO liga `reportSummarizer` no weekly: pré-agregação determinística (`buildMentalState`/`buildStudyWeek`) produz números/deltas/contagens bounded (cap 10 sessões), array cru `breakFeedbacks` fica FORA do prompt do LLM. **D-2 fetch** 2 fases — `gatherBundle` mantém `Promise.all` (16 fontes), depois coleta `sessionIds` das `grindSessions` e chama `getBreakFeedbacksBySessionIds(userId, sessionIds)` em `safe()` (reusa método existente, sem método storage novo). **D-3** `grindNotes` vive DENTRO de `mentalState` (coesão "estado da semana"). **D-4 thresholds** `fatigueSignal` = ≥2 sessões com `foco\|energia delta <= -2` (delta = last−first, sort ASC breakTime); cap 10 sessões; cap 500 chars/nota; cap 8 temas. **Shape** `ReportContent` ganha `mentalState?`/`studyWeek?` opcionais (lesson #7); popular qualquer um → `schemaVersion=2`; ausentes → markdown byte-idêntico ao atual (back-compat). Prompt `weeklyReport.ts` append no fim (prefixo estável, cache Anthropic lesson #10). SEM migration (interface TS pura). 2 diagramas em `diagrams/coach-est2/`. | Aceito | 2026-06-01 |
| [226](226-est5-interactive-monday-ritual.md) | **Interactive Monday Ritual (EST-5) — máquina de estados "Weekly Review" + montagem do PlanningWizard** — Costura EST-1/2/3/6 num ritual de segunda: recap (EST-2) → pedido de upload → análise profunda 7d → handoff p/ `startPlanning` (EST-6). Espelha EST-6 (orquestrador/rotas/shared/storage/tick + migration). **DEC-1** deep analysis + recap **determinísticos** (reusam `gatherBundle`/`buildMentalState`/`buildStudyWeek` do EST-2), **SEM Anthropic** neste sprint; ponto de extensão puro `buildDeepAnalysisNarrative(bundle)` (síncrono) p/ EST-5.1 trocar por `callReportLlm` sem mudar assinatura. **DEC-2** monta `<PlanningWizard>` numa **nova tab `'review'`** em `/coach-ai` (não rota nova — lesson #19; não na aba `reports`); `TabsTrigger` com `onClick` redundante (lesson #27); painel com ErrorBoundary local (lesson #29). **DEC-3** chave de semana resolvida pelo **server** (`POST .../start` retorna `weekStartDate`), nunca recalculada/divergente no client. **DEC-4** chave **UTC** (`ymdUtc`/`nextMondayUtc`) igual a `weekly_planning_sessions`; handoff passa a mesma chave (EST-6 faz a ponte UTC→BRT internamente — não unificar, CLAUDE.md §10). **Máquina de estados linear** `recap_sent→awaiting_upload→upload_confirmed→deep_analysis_done→planning` (somente avançar; salto/retrocesso → `invalid_weekly_review_transition`/409). **Idempotência** UNIQUE `(user_id,week_start_date)` + `onConflictDoNothing`+re-leitura; estados terminais = no-op. **Tick** `weeklyReviewMondayTick` (`0 * * * *`, molde `bImport.ts`): kill-switch `COACH_NUDGES_ENABLED=false` absoluto → `getLocalWeekday===1 && getLocalHour===MONDAY_HOUR(9)` (helpers JÁ existem em `timezone.ts` — sem extensão) → `getReportTier!=='free'` → `reportWeeklyEnabled` (reusa pref, sem coluna nova) → idempotência semanal. **Erros nomeados** `invalid_weekly_review_transition`/`weekly_review_not_found`/`tier_not_eligible`. **Colisão de rota** `registerCoachWeeklyReviewRoutes` ANTES de `registerCoachAi1bRoutes` (prefixo `weekly-review` disjunto) + guard test. **Achado:** `gatherBundle` é module-private em `weeklyReportGenerator.ts` → precisa virar `export` (aditivo). Migration **0089** `weekly_reviews` (UTC, UNIQUE, sem CHECK/FK/back-fill) PENDENTE PROD. 3 diagramas em `diagrams/coach-est5/`. | Aceito | 2026-06-01 |
| [228](228-fase-b-lead-measures-warmup-compliance-abgame-distribution.md) | **Fase B — Lead Measures Baratos (Warm-up Compliance + Distribuição A/B/C-game)** — Expõe na aba Mental do perfil os 2 lead measures 4DX já capturados e não mostrados: (1) compliance de warm-up (`warmup_rituals`) e (2) distribuição A/B/C-game (`cooldown_logs.abGameAnswers`). Leitura/agregação pura, **ZERO MIGRATION** (índices `idx_warmup_rituals_user_started` + `idx_cooldown_user_completed` já cobrem; interface TS pura). **D-B1** módulo de rota = reusar `cooldownAnalytics.ts` (NÃO novo `mentalAnalytics.ts`) — reusa helpers `userIdOf`/`resolvePeriod`/`setCacheHeader`, ordem de registro `index.ts:276` inalterada. **D-B2** handlers `(req,res)` com `import { storage }` direto (padrão do módulo, NÃO `injectedStorage` — lesson #34/#36 é p/ módulos novos; mock via `vi.mock`). **D-B3** tipos `WarmupComplianceMetrics`/`AbGameDistribution` exportados de `storage.ts` (promoção a `shared/` deferida). **D-B4** compliance warm-up: denominador = `grind_sessions status='completed'` (§6.1, comparável ao cooldown); numerador = `warmup_rituals completedAt!=null AND version='full' AND startedAt>=cutoff`; `decisionToPlay=false` conta POSITIVO por construção (travado founder — não filtra `decisionToPlay`); `complianceRate=total>0?min(1,completed/total):0` (clamp obrigatório — tabelas independentes, R1). `'minimal'` (W-3) estrito-`full` com TODO grepável (DEC-B1). **D-B5** auxiliares `abortedCount`/`decisionNotToPlayCount`/`overrideUsedCount` (DEC-B2 incluído); `fullCount` dropado (DEC-B3). **D-B6** A/B/C: parse defensivo (R2/lesson #9/#11), share só A vs B (C fora — travado founder); `cGameThemes=tokenizeLessons([...cGames,...lessons])` top-30 PII-safe (NUNCA texto cru — R5); vazio→200 não 404. **D-B7** 1 smoke test de registro basta (paths estáticos, sem `:param` — DEC-B8). **D-B8** NÃO pluga no motor de aderência ADR-227 (DEC-MA8 deferido) — só expõe; cabeçalhos citam `metas-tool` (sourceMetric futuro `warmup_compliance`/`a_game_pct`). 1 diagrama em `diagrams/fase-b-lead-measures/`. Sem migration, sem endpoint que altere captura. | Aceito | 2026-06-01 |
| [227](227-motor-aderencia-plano-vs-realizado.md) | **Motor de Aderência (plano-vs-realizado) — Fase A** — Serviço **puro, read-only, stateless, on-demand** (`server/coach/adherence/`) que compara o plano intencionado (EST-6: `weekly_planning_sessions.steps`, `study_weekly_plans`, `planned_tournaments`) com o realizado (`grind_sessions`, `study_sessions_v2`, `session_tournaments`) por janela. Contrato estável `getPlannedVsActual(userId, sourceMetric, period, injectedStorage?) → { planned, actual, compliancePct, dataSufficiency, breakdown }` (DEC-A7) importado pela Metas fatia-2 + recap EST-5. Distingue "pulado conscientemente" (`steps.status='skipped'` → `compliancePct=null`, A4) de "não feito" (`shortfall`). **DEC-MA1** tipo em `server/coach/adherence/types.ts` (server-only; promover a `shared/` quando client consumir). **DEC-MA2** `month`/`quarter` alinhados ao `period_start` dos reports (só `week` exercitado nesta fase). **DEC-MA3** realizado de `planned_tournaments_count` = `getSessionTournaments` (volume de sessão, §6.1), NÃO `tournaments` histórico. **DEC-MA4** `study_minutes` planejado = soma `steps.study` (fallback `study_weekly_plans.dailyTargetMinutes×dias`, `note='plan_from_weekly_plan'`). **DEC-MA5** `skipped` → `compliancePct=null` (decisão, não 100). **DEC-MA6 (bloqueador resolvido)** pluga em `buildWeeklyRecapContent` (`weeklyReviewOrchestrator.ts:125`) → `buildAdherenceRecap` antes de `buildRecap` (`buildRecap.ts:179`); `RecapInput.adherence?` opcional (lesson #7), `schemaVersion 1→2`; janela = semana anterior (`prevWeekPeriod:90`). **DEC-MA7** janela fechada quando `now >= weekStartDate+7d` UTC (senão `note='window_open'`, `low`). **DEC-MA8** `warmup_compliance` DEFERIDO (TODO grepável; fica no union mas SEM entrada no MAP → `unknown_source_metric`). **Achado de schema:** `planned_tournaments` é recorrente (`dayOfWeek`+`time`, SEM `scheduledDate`); `getGrindSessions`/`getStudySessionsV2` NÃO filtram status → motor filtra `status='completed'` in-memory. `getStatsLeaks` stub `[]` → `themes_focus_studied` degrada (`source_stub`/`low`, lesson #9). Chave UTC (`weekKeys.ts`); `lessons_recommended_done` converte UTC→BRT via `brtMondayYmd` (não unifica). **ZERO método storage novo, SEM MIGRATION** (interface TS pura, como EST-2). Sem endpoint, sem LLM. 2 diagramas em `diagrams/motor-aderencia/`. | Aceito | 2026-06-01 |
| [229](229-metas-1-fatia-1-goals-4dx-core.md) | **Ferramenta de Metas 4DX — fatia-1 (core: schema + CRUD + placar + snapshots)** — Entrega D1+D2+D3 do 4DX sem D4 (cobrança) e sem LLM: 3 tabelas novas + CRUD + placar read-only pace-line + snapshots semanais idempotentes (UTC) + UI `/metas`+`/metas/nova`. Realizado por **agregação direta read-only** (motor ADR-227 = dep OPCIONAL). **DEC-A6-impl (CRÍTICA) opção (b):** WIG **estende `career_goals`** via filha 1:1 `goal_wig_meta(career_goal_id PK/FK)` — `career_goals` fica **INTOCADA** (não formaliza no drizzle, não muda CHECK; **recon R2/R3:** `careerGoalsStorage` insere contra placeholder, débito latente AI-2B documentado e NÃO consertado). Medidas (D2) + links na tabela `goals` nova. **FK para `career_goals` = SQL-only** (drizzle = `varchar` sem `.references()` porque a tabela não está no drizzle). **Cap-2-WIG** conta só `career_goals` COM `goal_wig_meta` (WIG-4DX; legadas não contam). **DEC-A1** snapshot **lazy/on-read UPSERT** no `GET /scoreboard` (idempotente UNIQUE `(goal_ref_id, week_start_date)`) — **sem cron nesta fatia**; `COACH_NUDGES_ENABLED` não gateia leitura (governa proatividade; tick de cobrança = METAS-1.1). **DEC-A3** thresholds fixos `ratio=actual/expectedNow`: ahead>1.10, on_track[0.90,1.10], behind[0.70,0.90), at_risk<0.70, achieved se actual>=target; div-zero (expectedNow=0 dia-zero)→on_track ratio=null. **DEC-A8** ordem de rota: `/scoreboard`+`/templates/:profile/apply`+`/:id/link-measure`+`/:id/snapshots` ANTES de `/:id` (guard test via app real). **DEC-menores** PATCH rejeita `baselineValue`→400 `baseline_immutable`; link-measure duplicado = idempotente no-op 200 (ON CONFLICT DO NOTHING). Tier gating DEC-A2 (read-only todos; write `getReportTier!=='free'`). Storage attach `goalsStorage.ts` (lessons #34/#36); helpers puros `computePace.ts`+`aggregateCurrentValue.ts`. Migration **00XX** additive + rollback, PROD pendente. **FORA:** templates RF-14, RF-09/10/13/15, LLM, motor fino (fatia-2). 3 diagramas em `diagrams/metas-1-fatia-1/`. | Aceito | 2026-06-01 |
| [230](230-mda-1-reference-cards-nn-tagging.md) | **MDA (Tendências da População) como Tabela Dedicada + Junction N:N (MDA-1)** — Card de referência do exploit do pool num spot, **sem tempo/XP**, tagueado a **N temas (N:N)**, consultável enquanto estuda. Irmão do `stat_analysis` (EST-3/ADR-222) com tabela/rotas próprias. **D-1** `mda_reads` + junction `mda_read_themes` (espelha `study_theme_spot_links`) — NÃO `mode` em `study_sessions_v2`, NÃO array jsonb. Query "por tema" (QUENTE) = JOIN índice `(user_id, theme_id)`. **D-2** imagem privada reusa `createSpotImageStorage("private-uploads/mda")` (ADR-057), magic-bytes, cap 5MB/8 prints, 404 cross-user. **D-3** tagueio = diff idempotente (`UNIQUE (mda_read_id, theme_id)`, lesson #33) + tx fallback (#32). **D-4** cleanup de tags órfãs app-level em `deleteStudyTheme` (sem FK, padrão 0088/0089). **D-5** Zod-only sem CHECK; `themeIds` min1/max20; `patch` `.strict()`. **D-6** sem tier gate (paridade `stat_analysis`). Migration **0090** PENDENTE PROD. 2 diagramas em `diagrams/mda-1/`. Numerado 230 (não 229) por colisão com Metas-1 paralelo. | Aceito | 2026-06-01 |
| [231](231-fase-c-stats-leaks-behavioral-synthesis.md) | **getStatsLeaks: stub → detecção real de leak por síntese comportamental (Fase C #3)** — `getStatsLeaks(userId, top)` era stub `return []`, consumido por 5 fluxos read-only (study/biblioteca recs, weekly plan, auto-suggest de stats foco, motor de aderência) que nasciam vazios. **Achado:** as tabelas do TODO (`hud_stats_snapshots`/`stats_analyzer_history`) **não existem** — o Grindfy não importa valores de HUD jogados. Logo leak ≠ "valor jogado vs benchmark"; é **síntese comportamental** de 3 sinais já capturados: **S1** `coach_leak_focus` status='active' (peso 10/leak), **S2** `study_sessions_v2` mode='stat_analysis' últimos 60d !deleted (3×sessões + 1×min(entries_total,10), cap 30), **S3** `user_focus_stats` mês corrente (2/marca). Consolida por `statId` (severity somado); S1 sem match de catálogo → leak próprio `leak:<leakCode>`. Shape `{statId, statName, value:null, benchmark:number\|null, delta:null, severity, id?, type?, description?}` (value/delta sempre null — sem HUD number; benchmark = midpoint targetMin/Max do catálogo). **Helper PURO** `server/coach/leaks/detectLeaks.ts` (`synthesizeLeaks`, sem `new Date()`/drizzle/`@shared/schema` — D-A8/lesson #36) + `storage.getStatsLeaks` orquestra leitura (`Promise.allSettled` por fonte, degrade isolado + log antes do fallback lesson #9) e delega. `isValidStatId` delega ao `server/coach/statId.ts` compartilhado (MDA-1). **ZERO migration** (tabelas já existem). Decisões D-A1..D-A8 resolvidas. 1 diagrama em `diagrams/fase-c-stats-leaks/`. | Aceito | 2026-06-01 |
| [232](232-fase-c-4-tilt-tipado-7-tipos-antidoto.md) | **Tilt tipado (7 tipos) + antídoto (Fase C #4)** — Curso D1 define 7 tipos psicológicos de tilt (`running_bad, injustice, hate_losing, mistake, entitlement, revenge, desperation`), cada um com gatilho + **antídoto** distinto (Tendler/Angelo). A captura atual (`cooldown_logs.tiltSelfAssessment`) tem score + gatilhos situacionais (`TILT_TRIGGERS`) + texto livre, mas **não tipifica** nem oferece antídoto. **D-1** taxonomia = catálogo estático `shared/tilt-types.ts` (paridade `hud-stat-catalog.ts`; `as const`→`z.enum`; conteúdo literal do curso) + helper PURO `suggestTiltType` (heurística determinística trigger→tipo). **D-2** tipagem **híbrida**: jogador escolhe `tiltType` no cool-down; se não escolher, heurística SUGERE (nunca auto-grava — lesson #11). **D-3** estende `tiltSelfAssessmentSchema` com `tiltType?` nullable (lesson #7) — vive no jsonb existente, **SEM migration**. **D-4** agregação `getTiltTypeDistribution` conta SÓ `tiltType` explícito (não deriva heurística pros legados); `sharePct=count/typedCount` (fração), empate topo→`dominant:null`, `dataSufficiency='low'` se `typedCount<3`. **D-5** rota `GET /api/analytics/tilt-type-distribution` reusa `cooldownAnalytics.ts` (Fase B). PII: `action`/notas NUNCA na resposta (R5). Precedência heurística `revenge>injustice>running_bad>desperation(keptTilting>=7)>null`. UI: seletor+antídoto em `BlockThreeTiltReview` + widget `mental-tilt-dominant` em `MentalAnalyticsTab`. 1 diagrama em `diagrams/fase-c-4-tilt-tipado/`. | Aceito | 2026-06-02 |
| [233](233-fase-c-10-mental-result-insights.md) | **Insight mental ↔ resultado (sinais mentais × P&L) (Fase C #10)** — Cruza 3 sinais mentais já capturados com o P&L da sessão, read-only, sem fabricar significância. **Cruzamentos:** (RF-01) tilt type × P&L, (RF-02) foco da sessão × P&L (faixas alto≥7.5/médio≥5/baixo), (RF-03) A-game vs B/C-game × P&L. **Fonte P&L = `grind_sessions.profit`** (USD-equiv via fxResolver; precedente `getVarianceVsExpected`; §6.1 nível-sessão, NÃO `tournaments`; `session_tournaments` sem coluna currency). **D-1** 1 método storage `getMentalResultInsights` (1 scan + FX 1× + joins via inArray) computa os 3 + 3 endpoints finos fatiam sub-bloco (paridade Fase B; **3× recompute na abertura da aba documentado como follow-up — fix=1 endpoint deferido**). **D-3** FX resolvido no storage (lesson #6/#9), helper recebe USD pronto. **D-4** helper PURO `server/coach/mental/mentalResultInsights.ts` (sem drizzle/schema/`new Date()` — paridade detectLeaks). Honestidade: só média/mediana + N + `dataSufficiency` (low se bucket n<4 OU baseline n<8); `deltaVsBaseline` 0 em empate, null se um lado null; sem p-value. PII: `action`/notas NUNCA na resposta (R5). Cooldown filtra `completedAt!=null`. UI: 3 cards em `MentalAnalyticsTab`. SEM migration. 1 diagrama em `diagrams/fase-c-10-mental-resultado/`. | Aceito | 2026-06-02 |
| [234](234-metas-2-fatia-2-adherence-bridge-leak-focus.md) | **Metas 4DX fatia-2 — compliancePct rigoroso (motor de aderência) + leak_focus (METAS-2)** — Pluga o motor ADR-227 `getPlannedVsActual` pro compliancePct rigoroso (distingue "pulado conscientemente" de "não feito") + habilita a categoria `leak_focus` via getStatsLeaks real (ADR-231). Aditivo (lesson #7), **SEM migration** (cols `compliance_pct`/`data_sufficiency` do snapshot já existiam, antes gravava `'ok'` hardcoded). **D-1** ponte de vocabulário pura `server/coach/goals/adherenceBridge.ts` (nomes fatia-1 `sessions_per_week`... → sourceMetric do motor `grind_sessions_count`...; migram pro motor: sessions_per_week/grind_days/study_minutes_week/study_sessions_count; ficam diretas: roi_pct/abi/itm_pct/bankroll_usd §6.1). **D-3** helper PURO `leakFocusProgress.ts` (progresso HONESTO DEC-4: `coach_leak_focus.status='resolved'`→achieved; vazio→low+log; esforço=stat_analysis count; **NUNCA** severity/delta — value/delta são null). **D-4** statId alvo via sufixo `sourceMetric="leak_focus_progress:<statId>"` (sem coluna nova; valida `isValidStatId`). Scoreboard 3-way (motor/leak/direto) aditivo + snapshot grava compliancePct/dataSufficiency reais. Isolamento por medida (Promise.all + catch por fonte, log antes #9). **HIGH-1 reviewer corrigido:** trocado `findActiveLeakFocusList` (filtrava active+mês → resolved nunca chegava, mock idealizado escondia #3) por novo `findLeakFocusList` status-agnóstico. Follow-up doc (D-2): N× getPlannedVsActual por scoreboard (cap 3, dívida leve). 1 diagrama em `diagrams/metas-2-fatia-2/`. | Aceito | 2026-06-02 |
| [235](235-fase-d-stop-loss-cold-commit.md) | **Stop-loss pré-comprometido a frio (Fase D #5)** — Curso D5: o stop-loss deve ser decidido A FRIO (no warm-up, antes de jogar), não quente/tiltado (antídoto do tilt `desperation` ADR-228/#4). **REUSA** a infra existente (ADR-060 stopService + `user_settings.stopLossUsd` + enforcement 423/lock) — NÃO reconstrói. **D-1** cold-commit escreve direto em `user_settings.stopLossUsd` (sem coluna USD nova) + sinal frio em `warmup_rituals.sessionIntention.coldStopCommit` (jsonb). **SEM migration.** **D-2** heurística BI→USD: `nBI × abiUsd` (helper PURO `shared/stops/suggestColdStopLossUsd.ts`; ABI de getDashboardAllTimeSummary; FX→USD no caller lesson #6; default 3 BI; sugere não impõe #11; degrada sem ABI). **RF-04 "inegociável"** = trava de edição quente (`canLoosenStopLoss` puro): sessão ativa → afrouxar/remover BLOQUEADO 409 `STOP_LOOSEN_BLOCKED`, apertar/criar OK; a frio livre. **D-3** `coldStopCommitZod` compartilhado (`shared/cold-stop-commit.ts`) em lockstep nos 2 Zod de sessionIntention (lesson #28). UI: cold-commit no bloco Intenção do warm-up (WarmUpRunner→IntentionBlock→useWarmupRitual injeta no POST) + `StopRemainingIndicator` "quanto falta" (`computeStopRemaining` puro, state no_limit/losing/safe/breached) no grind-live. **Semântica documentada:** cold-commit substitui o stopLossUsd standing (modelo D5 "decide o stop a cada sessão", aceito; sem reset noturno do limite). Reviewer HIGH-1 (wiring morto WarmUpRunner) corrigido + MEDIUM-1 breached/MEDIUM-2 fmt. 1 diagrama em `diagrams/fase-d-stop-loss/`. | Aceito | 2026-06-02 |
| [AI-001](../ai-coach/adr-001-llm-provider.md) | Usar Claude API (Anthropic) como provedor LLM para AI Coach — **§"3 coaches especializados" SUPERSEDED por ADR-148** (agente unico); o resto (Sonnet 4.6 chat + Haiku 4.5 background) permanece | Proposto/parcialmente superseded | 2026-04-08 |
| [AI-002](../ai-coach/adr-002-memory-architecture.md) | Estrategia de memoria persistente com perfil + resumos + compactacao — **estendido por ADR-151** (a memoria agora tem um componente ESTRUTURADO em `users.ai_structured_profile`, alem da prosa em `user_ai_profile.content`) | Proposto + estendido | 2026-04-08 |

## Convencoes

- **Formato:** Um arquivo Markdown por decisao
- **Numeracao:** Sequencial (001, 002, 003...)
- **Status possiveis:** Proposto, Aceito, Deprecado, Substituido por ADR-XXX
- **Nunca deletar:** Marcar como Deprecado ou Substituido
- **Sempre incluir:** Opcoes descartadas com pros e contras

## Notas

- **Numeracao duplicada (047, 048, 051, 052, 106, 107):** existem dois ADRs com cada um desses
  numeros — sufixo `a`/`b` ou `-news` adicionado neste indice apenas para desambiguacao visual; os
  arquivos no disco mantem nome original (e.g. `047-summary-inline-reconcile.md` e
  `047-tts-browser-native-vs-cloud.md`; `106-home-daily-insight-rule-engine.md` e
  `106-news-feed-grok-integration.md`; `107-home-profile-detection-smart-auto-adapt.md` e
  `107-news-rss-x-search-refactor.md`). Divida historica de coordenacao entre sprints
  paralelos (TTS, Bankroll, Stats Analyzer, Spot Screenshots, home-reform-3 vs news-1, news-3) —
  proximos ADRs evitam colisao.
- **Branches paralelas:** ADRs 051-spot-screenshots, 052-spot-screenshots e
  053-spot-screenshots existem somente em `feature/spot-screenshots`. ADRs
  051-stats-analyzer e 052-stats-analyzer existem em `feature/stats-analyzer`. ADRs
  054-056 (Sprint F4 PrimeDope) sao independentes e podem coexistir em ambas branches
  sem conflito de numeracao.
- **Renumeracao 2026-05-02 (orphan-merge):** ADR-057 (hud-stat-targets) renumerado
  para **088** + ADR-058 (hud-snapshot-sample-size) renumerado para **089** apos
  merge da branch `feature/stats-analyzer-f4` (numbers 057/058 ja ocupados por
  spot-image-storage e auto-snapshot-cooldown em main).
- **Hiato 003 → 007:** ADRs 004-006 reservados/nunca escritos durante consolidacao
  inicial.
