🔧 MODAL SESSÃO - CORREÇÃO SIMPLES
ETAPA ÚNICA: MELHORIAS MÍNIMAS (15 min)
1.1 JSX - Manter Estrutura Original + Pequenos Ajustes
tsx<Dialog open={showConflictDialog} onOpenChange={setShowConflictDialog}>
  <DialogContent className="sm:max-w-[500px] bg-poker-surface border-gray-700">
    <DialogHeader>
      <DialogTitle className="text-white">🎯 Sessão em Andamento</DialogTitle>
      <DialogDescription className="text-gray-400">
        Você já tem uma sessão ativa para hoje. Escolha como deseja continuar:
      </DialogDescription>
    </DialogHeader>
    
    {conflictingSession && (
      <div className="bg-blue-900/20 border border-blue-600/50 rounded p-4">
        <p className="text-blue-400 font-medium">
          📅 Sessão de {formatImprovedDate(conflictingSession.date)}
        </p>
        <p className="text-gray-400 text-sm mt-1">
          Iniciada às {formatTime(conflictingSession.date)}
        </p>
      </div>
    )}
    
    <div className="flex flex-col gap-3 mt-4">
      {/* Botão Recomendado */}
      <Button
        onClick={handleConflictEditSession}
        className="w-full bg-blue-600 hover:bg-blue-700 text-white"
      >
        🎮 Ir para Sessão Existente
        <span className="ml-2 text-xs bg-blue-500/30 px-2 py-1 rounded">Recomendado</span>
      </Button>
      
      {/* Botão Secundário */}
      <Button
        onClick={handleConflictCreateNew}
        variant="outline"
        className="w-full border-green-600/50 text-green-400 hover:bg-green-600/10"
      >
        ➕ Criar Nova Sessão e Substituir
      </Button>
      
      {/* Botão Cancelar */}
      <Button
        onClick={handleConflictCancel}
        variant="ghost"
        className="w-full text-gray-400 hover:text-gray-300 text-sm"
      >
        ❌ Cancelar
      </Button>
    </div>
  </DialogContent>
</Dialog>
1.2 Funções Auxiliares (Adicionar apenas se não existirem)
tsx// Só adicionar se não existirem no código
const formatImprovedDate = (dateString: string) => {
  const date = new Date(dateString);
  const today = new Date();
  
  if (date.toDateString() === today.toDateString()) {
    return "Hoje";
  }
  
  return date.toLocaleDateString('pt-BR');
};

const formatTime = (dateString: string) => {
  return new Date(dateString).toLocaleTimeString('pt-BR', {
    hour: '2-digit',
    minute: '2-digit'
  });
};
1.3 CSS Mínimo (Opcional)
css/* Apenas se quiser adicionar transições suaves */
.bg-poker-surface button {
  transition: all 0.2s ease-out;
}

.bg-poker-surface button:hover {
  transform: translateY(-1px);
}
✅ O QUE MUDOU (MINIMAL)
Mantive Igual:

✅ Estrutura DialogContent original
✅ Classes CSS existentes
✅ Todos os handlers originais
✅ Layout responsivo

Mudanças Mínimas:

🎯 Título: "Sessão em Andamento"
🎮 Botão: "Ir para Sessão Existente"
📅 Card azul em vez de amarelo
➕ Ícones simples nos botões
🏷️ Badge "Recomendado" pequeno
📅 Data como "Hoje" + horário

Removido:

❌ Volume e Profit do card
❌ Animações complexas
❌ Classes CSS customizadas
❌ Estruturas complicadas

🔧 IMPLEMENTAÇÃO SEGURA
Passo 1: Substituir apenas o conteúdo do DialogContent
Passo 2: Adicionar as 2 funções de formatação
Passo 3: Testar
Se der problema: É só reverter o DialogContent para o original!
Esta versão mantém 95% do código original e só melhora o essencial. Muito mais segura! 🛡️