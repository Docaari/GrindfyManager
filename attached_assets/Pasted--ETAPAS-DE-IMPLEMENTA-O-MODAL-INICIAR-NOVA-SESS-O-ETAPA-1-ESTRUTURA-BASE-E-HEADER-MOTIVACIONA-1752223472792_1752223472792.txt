📝 ETAPAS DE IMPLEMENTAÇÃO - MODAL INICIAR NOVA SESSÃO

ETAPA 1: ESTRUTURA BASE E HEADER MOTIVACIONAL (2-3 horas)
1.1 Atualizar Componente Base
tsx// GrindSession.tsx - Modal de Iniciar Sessão
const StartSessionModal = ({ isOpen, onClose, onSuccess }: {
  isOpen: boolean;
  onClose: () => void;
  onSuccess: (sessionData: any) => void;
}) => {
  const [preparationPercentage, setPreparationPercentage] = useState(50);
  const [preparationNotes, setPreparationNotes] = useState("");
  const [dailyGoals, setDailyGoals] = useState("");
  const [screenCap, setScreenCap] = useState(10);
  
  // Título dinâmico baseado no horário
  const getTimeBasedContent = () => {
    const hour = new Date().getHours();
    
    if (hour >= 6 && hour < 12) {
      return {
        title: "Bom Dia, Grinder!",
        subtitle: "Vamos começar o dia dominando as mesas",
        motivation: "O early bird pega os peixes! 🌅",
        icon: "🌅"
      };
    } else if (hour >= 12 && hour < 18) {
      return {
        title: "Hora do Show!",
        subtitle: "As mesas estão aquecidas e prontas",
        motivation: "Prime time para maximizar o grind! 🔥",
        icon: "🔥"
      };
    } else {
      return {
        title: "Grind Noturno!",
        subtitle: "A noite é nossa, vamos conquistar",
        motivation: "Night owl mode ativado! 🦉",
        icon: "🦉"
      };
    }
  };

  const timeContent = getTimeBasedContent();

  return (
    <Dialog open={isOpen} onOpenChange={onClose}>
      <DialogContent className="modal-container epic-start-modal">
        {/* Header épico */}
        <div className="modal-header epic-header">
          <DialogClose className="close-btn">✕</DialogClose>
          <div className="header-icon animate-card-shuffle">{timeContent.icon}</div>
          <h2 className="modal-title gradient-text">{timeContent.title}</h2>
          <p className="modal-subtitle">{timeContent.subtitle}</p>
          <p className="motivation-text">{timeContent.motivation}</p>
        </div>
        
        {/* Body será implementado nas próximas etapas */}
      </DialogContent>
    </Dialog>
  );
};
1.2 CSS Classes Base
css/* Adicionar ao arquivo de estilos */
.epic-start-modal {
  @apply w-full max-w-lg bg-gray-900/95 border border-gray-700 rounded-3xl;
  @apply backdrop-blur-lg shadow-2xl overflow-hidden;
  animation: modalEnter 0.6s cubic-bezier(0.4, 0, 0.2, 1);
}

.epic-header {
  @apply p-8 text-center relative;
  background: linear-gradient(135deg, rgba(0, 255, 136, 0.1) 0%, rgba(0, 136, 255, 0.1) 100%);
}

.header-icon {
  @apply text-5xl mb-4 block;
}

.animate-card-shuffle {
  animation: cardShuffle 2s ease-in-out infinite;
}

.gradient-text {
  @apply text-3xl font-bold mb-2;
  background: linear-gradient(135deg, #00ff88 0%, #00dd77 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.modal-subtitle {
  @apply text-gray-400 mb-2;
}

.motivation-text {
  @apply text-green-400 font-semibold text-sm;
}

/* Animações */
@keyframes modalEnter {
  from {
    opacity: 0;
    transform: translateY(50px) scale(0.9);
  }
  to {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}

@keyframes cardShuffle {
  0%, 100% { transform: rotateY(0deg); }
  50% { transform: rotateY(180deg); }
}
1.3 Overlay Dinâmico
tsx// Hook para mudar background baseado na preparação
const useEpicBackground = (prepPercentage: number) => {
  useEffect(() => {
    const overlay = document.querySelector('.modal-overlay');
    
    if (prepPercentage >= 80) {
      overlay?.classList.add('high-energy');
      overlay?.classList.remove('medium-energy');
    } else if (prepPercentage >= 60) {
      overlay?.classList.add('medium-energy');
      overlay?.classList.remove('high-energy');
    } else {
      overlay?.classList.remove('high-energy', 'medium-energy');
    }
  }, [prepPercentage]);
};