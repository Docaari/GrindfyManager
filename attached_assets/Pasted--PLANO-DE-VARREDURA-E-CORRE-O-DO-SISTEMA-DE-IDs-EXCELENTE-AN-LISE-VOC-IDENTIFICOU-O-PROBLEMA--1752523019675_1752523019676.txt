🎯 PLANO DE VARREDURA E CORREÇÃO DO SISTEMA DE IDs
✅ EXCELENTE ANÁLISE! VOCÊ IDENTIFICOU O PROBLEMA RAIZ
O problema está na inconsistência de IDs em todo o sistema. Vamos fazer uma correção sistemática.

📋 COMANDO PARA VARREDURA E CORREÇÃO
🔧 PARA O DEV - AUDITORIA COMPLETA DO STORAGE.TS:
AÇÃO SISTEMÁTICA - VARREDURA DE IDs NO STORAGE:

1. AUDITORIA COMPLETA DO ARQUIVO STORAGE.TS:
   - Identificar TODAS as funções que usam userId
   - Mapear quais usam user.id vs user.userPlatformId
   - Listar inconsistências encontradas
   - Priorizar funções relacionadas a duplicação/upload

2. FOCO CRÍTICO - SISTEMA DE DUPLICAÇÃO:
   - Função isDuplicateTournament: usa qual campo?
   - Queries de tournaments: filtram por user_id ou userPlatformId?
   - Função de upload: salva com qual ID?
   - Verificação de existência: usa qual campo para buscar?

3. MAPEAMENTO DE INCONSISTÊNCIAS:
   - Listar funções que usam users.id
   - Listar funções que usam user.userPlatformId
   - Identificar onde há mistura de campos
   - Priorizar correções por impacto

4. CORREÇÃO PRIORITÁRIA - DUPLICAÇÃO:
   - Todas as queries relacionadas a tournaments devem usar userPlatformId
   - isDuplicateTournament deve receber e usar userPlatformId
   - Upload deve salvar com userPlatformId
   - Busca de torneios deve filtrar por userPlatformId

5. IMPLEMENTAR PADRÃO ÚNICO:
   - TODAS as funções devem usar userPlatformId
   - ELIMINAR uso de users.id como FK
   - Padronizar campo user_id em tabelas = userPlatformId
   - Garantir consistência absoluta

🔍 ÁREAS CRÍTICAS PARA VERIFICAÇÃO:
PRIORIDADE MÁXIMA:
FUNÇÕES DE DUPLICAÇÃO E UPLOAD:
1. isDuplicateTournament() - usa qual ID?
2. createTournament() - salva com qual ID?
3. getTournaments() - filtra por qual ID?
4. Queries WHERE user_id = ? - usa qual valor?
5. Upload de CSV - associa a qual ID?
PRIORIDADE ALTA:
TODAS AS FUNÇÕES DE TOURNAMENT:
- getTournamentsByUser()
- updateTournament()
- deleteTournament()
- getTournamentStats()
- Qualquer query que filtre por usuário

📊 RELATÓRIO SOLICITADO:
FORNEÇA MAPEAMENTO COMPLETO:
PARA CADA FUNÇÃO NO STORAGE.TS:

1. NOME DA FUNÇÃO
2. USA user.id OU user.userPlatformId?
3. CAMPO DA TABELA que filtra (user_id = ?)
4. VALOR passado para a query
5. INCONSISTÊNCIA identificada (se houver)
6. CORREÇÃO necessária

EXEMPLO:
- Função: isDuplicateTournament
- Usa: user.id (ERRADO)
- Campo: tournaments.user_id
- Valor: user.id (DEVERIA SER userPlatformId)
- Correção: Mudar para userPlatformId