Etapa final - vamos adicionar o botão na sidebar e finalizar todas as integrações:
🎯 Objetivo ETAPA 4:
Adicionar botão "Sugerir Melhoria" na sidebar e conectar todo o sistema.
📍 Botão na Sidebar:
[... outros itens ...]

├── Assinatura
├── Configurações  
├── Reportar Bug
├── [NOVO] Sugerir Melhoria  ← Adicionar aqui
└── Sair
🎯 Detecção Automática de Página (URLs Corretas):
Lógica de captura:
typescript// Capturar página atual automaticamente
const getCurrentPage = () => {
  const url = window.location.pathname;
  
  // Mapeamento de URLs para páginas do Grindfy
  if (url.includes('/dashboard')) return 'Dashboard';
  if (url.includes('/upload')) return 'Import';
  if (url.includes('/library')) return 'Biblioteca';
  if (url.includes('/coach')) return 'Grade';
  if (url.includes('/grind-live')) return 'Grind Ativo';
  if (url.includes('/grind')) return 'Grind';
  if (url.includes('/mental')) return 'Warm Up';
  if (url.includes('/planner')) return 'Calendario';
  if (url.includes('/estudos')) return 'Estudos';
  if (url.includes('/calculadoras')) return 'Ferramentas';
  if (url.includes('/analytics')) return 'Analytics';
  if (url.includes('/admin/users')) return 'Usuarios';
  if (url.includes('/bugs')) return 'Bugs';
  
  // Se não conseguir detectar ou der erro, usar "Outro"
  return 'Outro';
};
🔧 Especificações do Botão:

Posição: Entre "Reportar Bug" e "Sair" (parte inferior da sidebar)
Ícone: 💡 Lightbulb
Texto: "Sugerir Melhoria"
Ação: Abrir modal "Sugerir Melhoria" + detectar página atual
Estilo: Mesmo padrão dos outros botões da sidebar

Comportamento do Modal:

Ao abrir modal: Detectar página atual usando URLs corretas e pré-selecionar no dropdown
Fallback seguro: Se detecção falhar → selecionar "Outro" automaticamente
Usuário pode alterar: Dropdown permanece editável

🔄 Integrações Finais:
1. Conectar modais às abas:

Itens enviados devem aparecer na aba "Aguardando Análise"
Refresh automático das listas após envio
Invalidação de cache das queries

2. Sistema de ações nos cards:

Menu 3 pontos com ações: Visualizar, Editar, Aprovar/Rejeitar, Marcar como Concluído
Fluxo de aprovação funcional (análise → pendente → concluído)
Classificação de urgência (baixa/média/alta) para items aprovados

3. Funcionalidades do admin:

Aprovar item da análise → move para "Pendentes" + define urgência
Marcar como concluído → move para "Concluídos"
Rejeitar item → remove ou marca como rejeitado

4. Dashboard reativo:

Contadores atualizados em tempo real
Seções recentes sempre atualizadas
Métricas corretas baseadas nos status reais

⚠️ Importante:

URLs corrigidas conforme estrutura real do Grindfy
Detecção robusta com fallback para "Outro"
Testar em todas as páginas para garantir mapeamento correto

Resultado esperado: Sistema completo de bugs e melhorias com detecção inteligente usando as URLs corretas do Grindfy!