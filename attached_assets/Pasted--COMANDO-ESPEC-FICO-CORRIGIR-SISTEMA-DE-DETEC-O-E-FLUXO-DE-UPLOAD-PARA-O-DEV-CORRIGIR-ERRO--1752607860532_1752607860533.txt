🔧 COMANDO ESPECÍFICO: CORRIGIR SISTEMA DE DETECÇÃO E FLUXO DE UPLOAD
📋 PARA O DEV - CORRIGIR ERRO E IMPLEMENTAR FLUXO CORRETO:
"Corrija o erro 'Falha ao detectar duplicatas' e implemente o fluxo correto de upload:"
1. CORRIGIR ERRO DE DETECÇÃO DE DUPLICATAS:
PROBLEMA: "Falha ao detectar duplicatas"
DEBUG NECESSÁRIO:

A. VERIFICAR LOGS DO ERRO:
   - Que erro específico está acontecendo?
   - API /api/detect-duplicates retorna que status?
   - Dados estão sendo enviados corretamente?
   - userPlatformId está chegando na API?

B. VERIFICAR ENDPOINT:
   - Endpoint /api/detect-duplicates existe?
   - Está recebendo arquivo e userPlatformId?
   - Parser está funcionando sem salvar dados?
   - Retorna estrutura esperada?

C. CORRIGIR PROBLEMA ENCONTRADO:
   - Se erro 500: corrigir código do backend
   - Se erro 401: corrigir autenticação
   - Se erro de parsing: ajustar parser
   - Se erro de dados: verificar formato esperado
2. IMPLEMENTAR FLUXO CORRETO DE UPLOAD:
FLUXO ESPERADO (SEM BOTÃO):

A. USUÁRIO SELECIONA ARQUIVO:
   onChange do input file → inicia processo automaticamente

B. FASE 1 - ANÁLISE (Loading):
   - Mostrar: "Analisando arquivo... ⏳"
   - Mostrar: "Tempo estimado: X segundos"
   - Enviar arquivo para /api/detect-duplicates
   - NÃO salvar dados ainda

C. FASE 2 - RESULTADOS:
   Exibir:
   📊 Torneios encontrados: 150
   🔄 Torneios duplicados: 25  
   ❌ Torneios ignorados: 5
   
   Botões:
   [Importar Todos] [Importar Apenas Não Duplicados] [Cancelar]

D. FASE 3 - AÇÃO DO USUÁRIO:
   - "Importar Todos": salva todos os 150 torneios
   - "Importar Não Duplicados": salva apenas 125 (150 - 25)
   - "Cancelar": não salva nada
3. IMPLEMENTAR BACKEND PARA DETECÇÃO:
CRIAR/CORRIGIR ENDPOINT:

POST /api/detect-duplicates
Body: FormData com arquivo

RESPOSTA ESPERADA:
{
  success: true,
  analysis: {
    totalTournaments: 150,
    duplicates: 25,
    ignored: 5,
    validTournaments: [...], // dados parseados sem salvar
    duplicateDetails: [...] // lista dos duplicados
  },
  estimatedTime: "30 segundos"
}

LÓGICA:
1. Receber arquivo
2. Fazer parse completo
3. Verificar duplicatas (sem salvar)
4. Retornar estatísticas
5. Manter dados em memória/sessão para fase 3
4. IMPLEMENTAR FRONTEND:
FLUXO DO FRONTEND:

A. onChange do input file:
   - setLoading(true)
   - setAnalysisResults(null)
   - uploadForAnalysis(file)

B. Durante análise:
   - Mostrar spinner + "Analisando..."
   - Mostrar tempo estimado
   - Desabilitar input

C. Após análise:
   - setLoading(false)
   - setAnalysisResults(data)
   - Mostrar resultados + botões

D. Ação do usuário:
   - "Importar Todos" → POST /api/upload { action: "all" }
   - "Não Duplicados" → POST /api/upload { action: "nodups" }
   - "Cancelar" → limpar estado
5. CORRIGIR ESTRUTURA DE DADOS:
GARANTIR CONSISTÊNCIA:

A. DETECT-DUPLICATES:
   - NÃO salva no banco
   - Apenas analisa e retorna estatísticas
   - Mantém dados para próxima fase

B. UPLOAD FINAL:
   - Recebe dados já analisados
   - Salva conforme ação escolhida
   - Retorna resultado final

C. ISOLAMENTO:
   - Todas as operações filtradas por userPlatformId
   - Duplicatas verificadas apenas dentro do usuário
   - Zero vazamento entre contas
6. LOGS DE DEBUG:
ADICIONAR LOGS ESPECÍFICOS:
🔍 "ARQUIVO SELECIONADO: [nome] - [tamanho]"
🔍 "INICIANDO ANÁLISE PARA USER: [userPlatformId]"
🔍 "PARSE CONCLUÍDO: [total] torneios encontrados"
🔍 "DUPLICATAS DETECTADAS: [quantidade]"
🔍 "ANÁLISE COMPLETA: [tempo gasto]"
🔍 "USUÁRIO ESCOLHEU: [ação]"
🔍 "IMPORTAÇÃO FINAL: [resultado]"
Execute estas correções na ordem:

Primeiro: Debug e correção do erro "Falha ao detectar duplicatas"
Segundo: Implementar fluxo automático sem botão
Terceiro: Criar fase de análise com resultados
Quarto: Implementar botões de ação
Quinto: Testar fluxo completo

Reporte qual era o erro específico na detecção e como foi corrigido.