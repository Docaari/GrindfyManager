O parser está de fato agrupando corretamente os Reference IDs, mas o sistema ainda está salvando todos eles sem verificar se já existem no banco de dados.
Provavelmente está faltando uma validação no momento de persistência no banco, ou o controle de Reference ID não está sendo utilizado corretamente para deduplicação.

✅ Prompt final (refinado e definitivo) para corrigir o problema:
markdown
Copiar
Editar
⚠️ URGENTE: Corrigir importação duplicada de torneios Bodog

Mesmo após agrupar corretamente os dados da planilha `.xlsx` da Bodog com base no `Reference ID`, o sistema ainda está **salvando os mesmos torneios várias vezes** (com mesmo ID, buy-in e data).

---

### ✅ Regras atualizadas para parser da Bodog:

1. Cada torneio deve ser identificado unicamente pelo `Reference ID`.
2. Para cada `Reference ID`, devem ser extraídos:
   - Data (linha de Buy-in)
   - Buy-in (valor negativo do Buy-in)
   - Prêmio (valor positivo do Payout ou `0` se não houver)
3. O sistema deve montar a lista final de torneios **com base em `Reference ID` únicos**.

---

### 🛑 NOVA REGRA FUNDAMENTAL

Antes de salvar qualquer torneio processado, **verifique se o `Reference ID` já existe no banco** (ou cache da sessão).

- Se já existir → **ignorar o registro**
- Se for novo → salvar e registrar esse ID como importado

---

### 🔒 Exemplo de código (deve rodar antes de salvar os dados):

```ts
const torneiosUnicos = new Map();

for (const linha of planilha) {
  const id = linha.referenceId;
  const tipo = linha.descricao;
  const valor = parseFloat(linha.cashAmount);
  const data = linha.data;

  if (!id || !tipo || isNaN(valor)) continue;

  if (!torneiosUnicos.has(id)) {
    torneiosUnicos.set(id, {
      referenceId: id,
      data,
      buyin: null,
      premio: 0,
    });
  }

  const torneio = torneiosUnicos.get(id);

  if (tipo.includes("Buy-In")) {
    torneio.buyin = Math.abs(valor);
    torneio.data = data;
  }

  if (tipo.includes("Payout")) {
    torneio.premio = valor;
  }
}

const torneiosFinal = [];

for (const t of torneiosUnicos.values()) {
  if (t.buyin !== null) {
    // VALIDAR SE JÁ EXISTE NO BANCO
    if (await banco.torneioExiste(t.referenceId)) continue;

    torneiosFinal.push({
      nome: "MTT Bodog",
      data: formatarData(t.data),
      buyin: t.buyin,
      premio: t.premio,
      posicao: "N/A",
      site: "Bodog",
      categoria: "Vanilla",
      velocidade: "Normal",
      referenceId: t.referenceId,
    });
  }
}
🧪 Requisitos de validação após correção
Reference ID deve ser único no banco.

Nenhum torneio com o mesmo Reference ID pode ser salvo duas vezes.

Exporte log final com ✓ Skipped: ID já importado para casos ignorados.

📍Resumo:

O parser está funcionando. O que falta é a verificação no momento de salvar, para garantir que Reference IDs duplicados não sejam persistidos.